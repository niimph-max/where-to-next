# สเปค: คัดโมเมนต์ → รวมเป็นเล่ม

> เขียนไว้ 17 ก.ย. 2026 · อ้างอิงโค้ดเวอร์ชัน `2026.09.13a`
> เลขบรรทัดเป็นค่าประมาณ — **`grep` ยืนยันก่อนแก้เสมอ**

## สิ่งที่จะทำ

ให้ติดดาวโมเมนต์ที่เขียนไว้ดี → รวมโมเมนต์ที่คัดไว้เป็นเล่ม → เรียบเรียงเป็นตอน → ต่อเข้าระบบเล่ม A5 และ EPUB เดิม

**ท่อนหลัง (ตอน → เล่ม → EPUB) ใช้ของเดิมทั้งหมด ไม่ต้องแก้**

---

## ข้อตัดสินใจ (สรุปแล้ว)

| # | คำถาม | สรุป | เหตุผล |
|---|---|---|---|
| 1 | ดาวแยกจากหมวด? | **แยก** | `cat` = "เรื่องอะไร" · ดาว = "ดีแค่ไหน" คนละแกน ถ้ารวมกันจะเสียหมวดเดิมไป |
| 2 | 1 โมเมนต์ = 1 ตอน? | **หลายโมเมนต์รวมเป็น 1 ตอน** | ไม่งั้นได้หนังสือ 24 บท บทละครึ่งหน้า |
| 3 | เข้าเล่มแล้วยังอยู่ในฟีด? | **ยังอยู่** | เล่มแค่อ้างอิงถึง ไม่ย้ายออก |
| 4 | แก้ในเล่มแล้วโมเมนต์เปลี่ยนตาม? | **ไม่** | เล่มคือฉบับเรียบเรียง บันทึกดิบต้องคงเดิม — นี่คือคุณค่าของมัน |

---

## โครงข้อมูล

โมเมนต์เก็บเป็น array ของ object ใน `wtn-moments` (ดู `get moments()` ~6944, `saveMoments()` ~6945)

**เพิ่มฟิลด์เดียว:**

```js
{
  id, at, text, photos[], place, pos, wx, type, published,
  cat: 5,      // หมวดเดิม — ไม่แตะ
  star: 1      // ← เพิ่มใหม่ (1 = คัดแล้ว, ไม่มีฟิลด์/0 = ยังไม่คัด)
}
```

- ไม่ต้อง migrate — โมเมนต์เก่าไม่มี `star` ให้ถือว่าเป็น 0
- ไม่ต้องแก้ Supabase — `wtn-moments` ซิงก์ทั้งก้อนอยู่แล้ว (`ARR` ~3879 รู้จัก `wtn-moments` เป็น array ที่มี id ต่อชิ้น)
- **ต้องแตะ `_m: Date.now()` ทุกครั้งที่แก้** ไม่งั้น `_stampChanged` ไม่เห็นว่าเปลี่ยน

---

## เฟส 1 — ปุ่มดาว

### 1.1 ฟังก์ชัน toggle

วางใกล้ `deleteMoment()` (~7393) ใช้แพตเทิร์นเดียวกับที่มีอยู่แล้วตอน ~7034

```js
toggleStar(id) {
  this.saveMoments(this.moments.map(x => x.id === id
    ? { ...x, star: x.star ? 0 : 1, _m: Date.now() } : x));
  this.scheduleSync && this.scheduleSync();
  this.forceUpdate();
}
```

### 1.2 ส่งค่าเข้า template

ใน renderVals ของการ์ดโมเมนต์ (~14186–14192) เติมสองบรรทัด:

```js
starred: !!m.star,
toggleStar: () => this.toggleStar(m.id),
```

### 1.3 ปุ่มในเทมเพลต

วางในแถวหัวการ์ด (บรรทัดเดียวกับวันที่/หมวด) ชิดขวา

- ติดดาวแล้ว: `★` สีดินเผา `#c05f39`
- ยังไม่ติด: `☆` สีเส้น `#e6dbc7`
- พื้นที่แตะ ≥44px ตามกติกาในโครงการ
- ใช้ `<sc-if value="{{ m.starred }}">` สลับสองไอคอน — **ห้ามใส่นิพจน์ใน `{{ }}`** คำนวณใน renderVals แล้วส่งชื่อออกมา

---

## เฟส 2 — ชั้นเรื่องคัด

### 2.1 ตัวกรอง

ฟีดมี `catFilter` อยู่แล้ว (~14196) เพิ่ม `starOnly` แบบเดียวกัน:

```js
if (this.state.starOnly) momentList = momentList.filter(x => x.starred);
```

กรองซ้อนกับ `catFilter` ได้เลย ไม่ต้องแยกหน้า

### 2.2 ทางเข้า

เพิ่มชิป **"★ คัดไว้"** ในแถว segment เดิม (เรื่องเล่า / โมเมนต์ / คลังรูป / แผนที่)
โชว์จำนวนข้างชื่อ เช่น `★ คัดไว้ 24`

### 2.3 ปุ่ม "รวมเป็นเล่ม"

โผล่เฉพาะตอน `starOnly` เปิดอยู่ และมีโมเมนต์ที่คัด ≥ 3 อัน

---

## เฟส 3 — จัดกลุ่มเป็นเล่ม

### 3.1 แอปเสนอกลุ่มให้ก่อน

24 อันติ๊กทีละอันคือทรมาน — ให้แอปจัดมาก่อนแล้วผู้ใช้ค่อยแก้

เรียงลำดับการเสนอ:

1. **ตามสถานที่** — `place` ตรงกัน ≥ 3 อัน → *"แหลมสิงห์ · 6 โมเมนต์ · 2567–2569"*
2. **ตามหมวด** — `cat` เดียวกัน ≥ 3 อัน → *"หมวดผู้คน · 4 โมเมนต์"*
3. **ตามช่วงเวลา** — เดือน/ปีเดียวกัน ≥ 3 อัน
4. **ที่เหลือ** — กองรวม ให้ผู้ใช้ลากจัดเอง

โมเมนต์หนึ่งอันอยู่ได้หลายกลุ่ม — ผู้ใช้เลือกเองว่าจะเอากลุ่มไหน

### 3.2 ผู้ใช้แก้ได้ทุกอย่าง

เปลี่ยนชื่อกลุ่ม · ลากย้ายระหว่างกลุ่ม · เอาออก · สร้างกลุ่มเปล่า

**ห้ามบันทึกอัตโนมัติ** — ต้องกดยืนยันเสมอ (หลักการเดิมของโครงการ: ให้ AI ร่างก่อน คนยืนยันทีหลัง)

---

## เฟส 4 — เรียบเรียงเป็นตอน

**ขั้นนี้ข้ามไม่ได้** โมเมนต์เป็นบันทึก *ตอนนั้น* — เขียนขณะยังอยู่ตรงนั้น ยังไม่รู้ว่าเรื่องจะจบยังไง
หนังสือต้องการน้ำเสียงที่ *มองย้อนกลับไป* ถ้าเรียงต่อกันดิบๆ คนอ่านจะรู้สึกว่าอ่านโพสต์เก่าซ้ำ

### 4.1 หนึ่งกลุ่ม → หนึ่งตอน

```js
{
  id, title,           // ชื่อกลุ่ม แก้ได้
  gen: 'pick',         // ← ค่าใหม่ ของเดิมใช้ 'day1','day2'… (ดู ebChapterMap ~9192)
  srcIds: [...],       // id โมเมนต์ต้นทาง — อ้างอิงเท่านั้น ไม่ผูกสองทาง (ข้อ 4)
  html, cover, photos[]
}
```

`ebChapterMap()` แยกตอนด้วย `/^day(\d+)$/` อยู่แล้ว ตอนที่ไม่ match จะตกไปกอง `extra` — **`gen: 'pick'` ทำงานได้เลยโดยไม่ต้องแก้** แต่ถ้าอยากเรียงลำดับให้ดีขึ้นค่อยไปเติมทีหลัง

### 4.2 AI ช่วยตรงนี้

- ร่างคำเชื่อมระหว่างโมเมนต์
- เขียนเกริ่นบท
- เสนอชื่อตอน

แล้วนิ่มแก้ให้เป็นเสียงตัวเอง — **AI ร่าง คนตัดสิน**

### 4.3 เล่มไม่ผูกทริป

`ebStory()` / `ebTripId()` (~9166–9177) ตอนนี้บังคับต้องมี `tripId`
ต้องรองรับ story ที่ `tripId` เป็น null — เล่มคัดไม่ได้อยู่ในทริปไหน

ตรวจจุดที่เรียก `ebDayLabel()` และ `ebExpenses()` ด้วย — สองตัวนี้ต้องข้ามไปเมื่อไม่มีทริป

---

## ไม่ทำใน v1

- ไม่ทำดาวหลายระดับ (1–5 ดาว) — เอา มี/ไม่มี ก่อน
- ไม่ทำแชร์ชั้นเรื่องคัดให้คนอื่นดู
- ไม่แตะ `/b/` share worker — เล่มคัดยังไม่ต้องมีลิงก์แชร์
- ไม่ทำ auto-grouping ด้วย AI — ใช้กฎง่ายๆ ตามข้อ 3.1 ก่อน ดูว่าพอไหม

---

## เช็คลิสต์ทดสอบ

- [ ] ติดดาว → ปิดแอป → เปิดใหม่ ดาวยังอยู่
- [ ] ติดดาวบนมือถือ → เปิดเว็บ ดาวตามมา (ทดสอบการซิงก์ `_m`)
- [ ] โมเมนต์เก่าที่ไม่มีฟิลด์ `star` ต้องไม่พังและไม่ขึ้นดาว
- [ ] กรอง ★ + หมวด พร้อมกัน ได้ผลถูก
- [ ] เอาโมเมนต์เข้าเล่มแล้ว โมเมนต์ยังอยู่ในฟีดเหมือนเดิม (ข้อ 3)
- [ ] แก้ข้อความในตอน → โมเมนต์ต้นทางไม่เปลี่ยน (ข้อ 4)
- [ ] เล่มที่ไม่มีทริป เปิดอ่าน A5 ได้ ไม่ error
- [ ] ส่งออก EPUB จากเล่มคัดได้จริง
- [ ] บัญชีฟรี: ดาวใช้ได้ (อยู่ในเครื่อง) แต่ EPUB ยังเป็นพรีเมียมเหมือนเดิม

---

## ลำดับที่แนะนำ

**เฟส 1 อย่างเดียวก่อน แล้วใช้จริงสัก 2 สัปดาห์**

ปุ่มดาวเป็นงานเล็กมาก และจะตอบคำถามสำคัญที่สุดให้ก่อน: **พอมีปุ่มแล้วนิ่มกดจริงไหม?**

ถ้ากดแล้วสนุก ค่อยทำเฟส 2–4 · ถ้าไม่ค่อยได้กด แปลว่าโจทย์ไม่ใช่ "คัดยาก" แต่เป็นอย่างอื่น — รู้ตั้งแต่เสียเวลาไปนิดเดียว
