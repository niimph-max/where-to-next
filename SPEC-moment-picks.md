# สเปค: ทำเล่มจากของที่มีอยู่แล้ว

> เขียนไว้ 17 ก.ย. 2026 · เพิ่มส่วน WordPress 19 ก.ย. 2026 · บันทึกผลทดสอบ CORS 20 ก.ย. 2026
> อ้างอิงโค้ดเวอร์ชัน `2026.09.17a` · เลขบรรทัดเป็นค่าประมาณ — **`grep` ยืนยันก่อนแก้เสมอ**
> _(ชื่อไฟล์ยังเป็น `SPEC-moment-picks.md` ตามเดิม แต่ตอนนี้ครอบคลุมสองเรื่อง)_

เอกสารนี้ครอบคลุม **2 ทาง** ที่จะเปลี่ยนของที่บันทึกไว้แล้วให้เป็นเล่ม/EPUB

| ส่วน | ต้นทาง | สถานะ |
|---|---|---|
| **A** | โมเมนต์ที่คัดไว้ | เฟส 1 (ปุ่มดาว) ขึ้น repo แล้ว |
| **B** | เรื่องเล่าที่นำเข้าจาก WordPress | ทำเสร็จแล้ว `2026.09.20b–d` |

**ทั้งสองทางติดคอขวดเดียวกัน** → ดู "ส่วนกลาง" ข้างล่าง แก้ครั้งเดียวได้ทั้งคู่

---

# ส่วนกลาง · ปลดล็อกเล่มที่ไม่ผูกทริป

**สถานะ: ทำแล้ว 20 ก.ย. 2026 — เวอร์ชัน `2026.09.20a`**

~~นี่คืองานชิ้นเดียวที่บล็อกทั้งส่วน A และ B อยู่~~

ทุกอย่างในแอปตอนนี้ห้อยอยู่กับ **ทริป** — `ebStory()` / `ebTripId()` (~9178–9189) บังคับต้องมี `tripId`
และ `makeEpub()` (~11208) ตัดจบตั้งแต่บรรทัดแรกถ้าไม่มีวันในทริป:

```js
const D = await this.ebGather();
if (!D.days.length) { this.showToast('ยังไม่มีบันทึกในทริปนี้'); return; }
```

**ต้องทำ**

- รองรับ story ที่ `tripId` เป็น `null`
- `ebGather()` ต้องประกอบเล่มจาก "รายการตอน" ได้ ไม่ใช่เฉพาะ "วันในทริป"
- ตรวจจุดที่เรียก `ebDayLabel()` และ `ebExpenses()` — สองตัวนี้ต้องข้ามไปเมื่อไม่มีทริป
- ข้อความ error ต้องเปลี่ยน — "ยังไม่มีบันทึกในทริปนี้" ใช้ไม่ได้กับเล่มที่ไม่มีทริป

## สิ่งที่แก้ไปจริง (`2026.09.20a`)

**ต้นตอไม่ใช่แค่ "ทำไม่ได้" แต่เป็น "ได้เล่มผิด"** — `ebTripId()` เดิมจบด้วย
`return (s && s.tripId) || this.state.trip;` เรื่องที่ `tripId` เป็น `null` จึงหล่นไปหยิบ
**ทริปที่เลือกค้างไว้ในแอป** มาใช้ ทดสอบกับโค้ดเก่าแล้วได้ผลนี้จริง:

| | โค้ดเก่า | โค้ดใหม่ |
|---|---|---|
| ชื่อเล่ม | "คาซัคสถาน" | "บันทึกจากบล็อกเก่า" |
| ช่วงวัน | 24 ก.ค.-3 ส.ค. 2026 | 2 ตอน |
| ตอนแรก | "สนามบิน" (เช็คอินของทริปอื่น) | "วันแรกที่เกียวโต" |
| ค่าใช้จ่าย | ของทริปคาซัคสถาน | ไม่มี |

เป็นบั๊กเงียบ ไม่ error ไม่เตือน — ได้ไฟล์ออกมาแต่เนื้อในเป็นของทริปอื่น

**ที่แก้**

- `ebTripId()` — เรื่องที่ไม่ผูกทริปคืน `''` ไม่หล่นไปใช้ `state.trip`
- `ebStory()` + `_ebStoryOverride` — เปิดเล่มด้วย id ของเรื่องได้ ไม่ต้องมี tripId
- `ebKey()` ใหม่ — กุญแจเก็บปก/รูปที่จัดวาง ใช้ `'st' + storyId` เมื่อไม่มีทริป (ไม่งั้นเล่มไม่ผูกทริปทุกเล่มแชร์ช่องเดียวกัน)
- `ebGather()` — ข้าม `healCheckinDays` / เช็คอิน / ค่าใช้จ่าย / แผนที่ เมื่อไม่มีทริป · ชื่อ-ผู้เขียน-ปกมาจากเรื่อง · ปีบนปกคำนวณจากช่วงวันที่ของตอน
- `ebMapImage()` — คืน `null` ทันทีเมื่อไม่มีทริป
- `ebEstimate()` — เพิ่ม `extras` / `parts` แยกจาก `days` · UI ใช้ `parts` ตัดสินว่ามีเล่มไหม และเปลี่ยนคำจาก "N วัน" เป็น "N ตอน" เมื่อไม่มีทริป
- `shelfBooks()` + `shelfOpenStory()` — ชั้นหนังสือขึ้นเล่มจากเรื่องที่ไม่ผูกทริปด้วย
- `_epubAssets()` — **เลิก catch ว่าง** นับรูปที่ดึงไม่ได้ แยกตามโดเมน แล้วเตือนหลังไฟล์เสร็จ

---

# ส่วน A · โมเมนต์ที่คัดไว้ → เล่ม

## ข้อตัดสินใจ (สรุปแล้ว)

| # | คำถาม | สรุป | เหตุผล |
|---|---|---|---|
| 1 | ดาวแยกจากหมวด? | **แยก** | `cat` = "เรื่องอะไร" · ดาว = "ดีแค่ไหน" คนละแกน ถ้ารวมกันจะเสียหมวดเดิมไป |
| 2 | 1 โมเมนต์ = 1 ตอน? | **หลายโมเมนต์รวมเป็น 1 ตอน** | ไม่งั้นได้หนังสือ 24 บท บทละครึ่งหน้า |
| 3 | เข้าเล่มแล้วยังอยู่ในฟีด? | **ยังอยู่** | เล่มแค่อ้างอิงถึง ไม่ย้ายออก |
| 4 | แก้ในเล่มแล้วโมเมนต์เปลี่ยนตาม? | **ไม่** | เล่มคือฉบับเรียบเรียง บันทึกดิบต้องคงเดิม — นี่คือคุณค่าของมัน |

## โครงข้อมูล

โมเมนต์เก็บเป็น array ของ object ใน `wtn-moments` (`get moments()` ~6944, `saveMoments()` ~6945)

```js
{
  id, at, text, photos[], place, pos, wx, type, published,
  cat: 5,      // หมวดเดิม — ไม่แตะ
  star: 1      // ← เพิ่มใหม่ (1 = คัดแล้ว, ไม่มีฟิลด์/0 = ยังไม่คัด)
}
```

- ไม่ต้อง migrate — โมเมนต์เก่าไม่มี `star` ให้ถือว่าเป็น 0
- ไม่ต้องแก้ Supabase — `wtn-moments` ซิงก์ทั้งก้อนอยู่แล้ว (`ARR` ~3879)
- **ต้องแตะ `_m: Date.now()` ทุกครั้งที่แก้** ไม่งั้น `_stampChanged` ไม่เห็นว่าเปลี่ยน

## ✅ เฟส 1 — ปุ่มดาว (เสร็จแล้ว · `v.2026.09.17a`)

- `toggleStar(id)` วางก่อน `editMoment()` — สลับ 0↔1, แตะ `_m`, เรียก `scheduleCloudPush()`
- renderVals การ์ดโมเมนต์: `starred` / `starOn` / `starOff` / `toggleStar`
  (แยกสองธงเพราะ `{{ }}` ใส่นิพจน์ไม่ได้)
- ปุ่มดาวในเทมเพลต วางซ้ายปุ่ม "แชร์" — `<sc-if>` สลับดาวโปร่ง/ดาวทึบ
- ชิปกรอง **★ คัดไว้ N** แทรกหน้า `catFilters` — กรองซ้อนกับหมวดได้ โผล่เฉพาะตอนมีของให้กรอง

## เฟส 2 — จัดกลุ่มเป็นเล่ม

24 อันติ๊กทีละอันคือทรมาน — ให้แอปจัดมาก่อนแล้วผู้ใช้ค่อยแก้

1. **ตามสถานที่** — `place` ตรงกัน ≥ 3 อัน → *"แหลมสิงห์ · 6 โมเมนต์ · 2567–2569"*
2. **ตามหมวด** — `cat` เดียวกัน ≥ 3 อัน
3. **ตามช่วงเวลา** — เดือน/ปีเดียวกัน ≥ 3 อัน
4. **ที่เหลือ** — กองรวม ให้ลากจัดเอง

โมเมนต์หนึ่งอันอยู่ได้หลายกลุ่ม · **ห้ามบันทึกอัตโนมัติ** ต้องกดยืนยันเสมอ

## เฟส 3 — เรียบเรียงเป็นตอน

**ขั้นนี้ข้ามไม่ได้** โมเมนต์เป็นบันทึก *ตอนนั้น* หนังสือต้องการน้ำเสียงที่ *มองย้อนกลับไป*
ถ้าเรียงต่อกันดิบๆ คนอ่านจะรู้สึกว่าอ่านโพสต์เก่าซ้ำ

```js
{
  id, title,           // ชื่อกลุ่ม แก้ได้
  gen: 'pick',         // ← ค่าใหม่ ของเดิมใช้ 'day1','day2'… (ดู ebChapterMap ~9204)
  srcIds: [...],       // id โมเมนต์ต้นทาง — อ้างอิงเท่านั้น ไม่ผูกสองทาง (ข้อ 4)
  html, cover, photos[]
}
```

`ebChapterMap()` แยกตอนด้วย `/^day(\d+)$/` ตอนที่ไม่ match ตกไปกอง `extra` — **`gen: 'pick'` ทำงานได้เลยโดยไม่ต้องแก้**

**AI ช่วยตรงนี้:** ร่างคำเชื่อมระหว่างโมเมนต์ · เขียนเกริ่นบท · เสนอชื่อตอน — แล้วนิ่มแก้ให้เป็นเสียงตัวเอง

---

# ส่วน B · เรื่องเล่าจาก WordPress → EPUB

## สถานะ: ตัวสร้าง EPUB ใช้ได้จริงอยู่แล้ว

`_zipStore()` / `epubFiles()` (~10829–11100) เขียน EPUB 3 ตามสเปกจริง:

- zip แบบ store ให้ `mimetype` เป็นไฟล์แรกและไม่บีบอัด (ตามข้อบังคับของสเปก)
- `META-INF/container.xml` · `OEBPS/content.opf` · สารบัญ `nav` ที่มี `epub:type="toc"`
- XHTML ใส่ `epub:type="chapter"` · ปกใส่ `epub:type="cover"`

**เปิดใน Apple Books / Play Books / Kobo ได้จริง ส่วนนี้ไม่ต้องทำอะไรเพิ่ม**

## จุดที่ 1 — ติดคอขวดเดียวกับส่วน A

ดู "ส่วนกลาง" ข้างบน · เรื่องเล่าที่นำเข้ามาและไม่มีโครงวันในทริป จะได้ `D.days` ว่าง แล้วถูกตัดจบตั้งแต่บรรทัดแรก

## จุดที่ 2 — รูปยังอยู่บนเซิร์ฟเวอร์ WordPress

ตอนนำเข้า แอป**อ้างรูปจาก `files.wordpress.com` โดยตรง** ไม่ได้ก็อปมาเก็บ
(ตั้งใจออกแบบไว้ — ประหยัดพื้นที่ และรูปไม่ตายตอนโดเมนหมดอายุ · ดูข้อความในหน้านำเข้า ~1641)

**แต่ EPUB ต้องพึ่งตัวเอง** รูปต้องถูกดึงมาใส่ใน zip — `_epubAssets()` ~10877:

```js
const b = await (await fetch(u)).blob();
...
} catch (e) { /* รูปนี้ข้ามไป */ }
```

⚠️ **catch ว่าง** ถ้า `files.wordpress.com` ไม่ยอมให้ดึงข้ามโดเมน (CORS) รูปจะหลุดเงียบทีละใบ
→ **ได้ EPUB ที่ไม่มีรูปเลย โดยไม่มีใครบอก** — แพตเทิร์นเดียวกับบั๊กรูปหายที่แก้ไปใน `2026.09.13a`

### ผลทดสอบ CORS — ทดสอบแล้ว 20 ก.ย. 2026 · **ไม่ผ่าน**

ทดสอบจาก origin `https://onevela.net` กับรูปจริงใน `wtn-stories` (`lunarroom.files.wordpress.com`)

| วิธีเรียก | ผล |
|---|---|
| `<img src="...">` | **โหลดได้** 1200x800 — นี่คือเหตุผลที่รูปขึ้นในแอปปกติ |
| `fetch(u)` | **TypeError: Failed to fetch** — ไม่มี `Access-Control-Allow-Origin` |
| `fetch(u, {mode:'no-cors'})` | `type=opaque status=0` — อ่านเนื้อไฟล์ไม่ได้ |
| `<img crossOrigin="anonymous">` | **ถูกบล็อก** — ทางอ้อมผ่าน canvas ก็ตัน |

**สรุป: ฝั่ง client ทำไม่ได้ทุกทาง** ต้องผ่านเซิร์ฟเวอร์ของเราเท่านั้น
(ในเครื่องมี 562 ลิงก์รูป WordPress ใน `wtn-stories` — ถ้าไม่แก้ EPUB จะไม่มีรูปสักใบ)

### ⛔ ทางที่เคยคิดแล้วตัดทิ้ง — Worker ดึงรูป

เคยออกแบบไว้ว่าจะเพิ่มโหมดให้ Worker ที่ `onevela.net/i` ดึงรูปจาก URL ภายนอกฝั่งเซิร์ฟเวอร์
(ไม่ติด CORS) แล้วเก็บลง R2 ไปในตัว — **ตัดสินใจไม่ทำ 20 ก.ย. 2026**

เหตุผล: มันแก้ปัญหาให้**ผู้ใช้คนเดียว** เพราะไม่มีใครอื่นนำเข้าจาก WordPress
(ดู "ตัวนำเข้า WordPress = ของเจ้าของคนเดียว" ใน `CLAUDE.md`)
**อย่าเสนอใหม่** เว้นแต่เจ้าของเปลี่ยนใจเอง

### ✅ ทางที่ใช้จริง — แนบรูปของตัวเองเข้าตอน (`2026.09.20b`)

เจ้าของมีรูปสำรองอยู่แล้ว และ EPUB หนึ่งเล่มใส่รูปไม่กี่ใบ จึงไม่ต้องย้ายรูป 562 ใบ

- ปุ่ม **"＋ อัปรูปจากเครื่อง"** ในหน้า "จัดรูปในเล่ม" (`ebPickFiles()`)
- ย่อเหลือด้านยาว 1800px → `stashPhoto()` → ref `idb:` → `resolvePhoto()` คืน data url → **ฝังลง EPUB ได้จริง**
- รูปที่อัปเก็บเป็นคลังประจำเล่ม (`wtn-eb-pool` คีย์ด้วย `ebKey()`) หยิบไปวางจุดอื่นซ้ำได้
- ⚠ เก็บในเครื่องที่อัปเท่านั้น **ยังไม่ซิงค์ข้ามเครื่อง**

## ข้อได้เปรียบเหนือส่วน A

โพสต์บล็อก**เขียนจบแล้ว** — มีหัวเรื่อง มีย่อหน้า มีน้ำเสียงที่เรียบเรียงมาแล้ว
**ไม่ต้องมีขั้น "เรียบเรียงเป็นตอน" เหมือนโมเมนต์** → ส่วน B ใกล้เสร็จกว่าส่วน A

## เลือกเรื่องไหนเข้าเล่ม

โพสต์ 400+ อัน ไม่ได้อยากทำเป็นเล่มทุกอัน — **ใช้กลไกเดียวกับส่วน A ได้เลย: ติดดาว**
ถ้าตอนนำเข้าเก็บเป็นตอนในระบบเรื่องเล่า ก็เพิ่ม `star` ที่ระดับตอนแบบเดียวกัน ไม่ต้องคิดกลไกใหม่

---

# ไม่ทำในรอบนี้

- ดาวหลายระดับ (1–5 ดาว) — เอา มี/ไม่มี ก่อน
- แชร์ชั้นเรื่องคัดให้คนอื่นดู
- ไม่แตะ `/b/` share worker — เล่มคัดยังไม่ต้องมีลิงก์แชร์
- auto-grouping ด้วย AI — ใช้กฎง่ายๆ ก่อน ดูว่าพอไหม

---

# เช็คลิสต์ทดสอบ

**ส่วน A**
- [x] ติดดาว → ปิดแอป → เปิดใหม่ ดาวยังอยู่
- [x] โมเมนต์เก่าที่ไม่มีฟิลด์ `star` ไม่พังและไม่ขึ้นดาว
- [x] กรอง ★ + หมวด พร้อมกัน ได้ผลถูก
- [ ] ติดดาวบนมือถือ → เปิดเว็บ ดาวตามมา (ทดสอบการซิงก์ `_m`)
- [ ] เอาโมเมนต์เข้าเล่มแล้ว โมเมนต์ยังอยู่ในฟีดเหมือนเดิม (ข้อ 3)
- [ ] แก้ข้อความในตอน → โมเมนต์ต้นทางไม่เปลี่ยน (ข้อ 4)

**ส่วนกลาง**
- [x] เล่มที่ไม่มีทริป ประกอบได้ ไม่ error (ทดสอบ logic แล้ว — ยังต้องลองบนเครื่องจริง)
- [x] ข้อความ error เปลี่ยนแล้ว ไม่พูดถึง "ทริป" เมื่อเล่มไม่มีทริป

**ส่วน B**
- [x] ทดสอบ CORS ของ `files.wordpress.com` แล้ว — **ไม่ผ่าน** (20 ก.ย. 2026) บันทึกผลไว้ข้างบน
- [ ] EPUB จากเรื่องเล่า WordPress มีรูปครบ — **นับจำนวนรูปเทียบกับต้นฉบับ ไม่ใช่แค่เปิดดูผ่านๆ**
- [ ] ถ้ารูปหลุด ต้องมีข้อความบอก ไม่ใช่เงียบ
- [ ] เปิดไฟล์ใน Apple Books ได้จริง สารบัญใช้ได้

**ทั้งหมด**
- [ ] บัญชีฟรี: ดาวใช้ได้ (อยู่ในเครื่อง) แต่ EPUB ยังเป็นพรีเมียมเหมือนเดิม

---

# ลำดับที่แนะนำ

1. **ใช้ปุ่มดาวจริงสัก 2 สัปดาห์** — คำถามคือ *"กดจริงไหม"* ไม่ใช่ *"โค้ดทำงานไหม"*
2. ~~ทดสอบ CORS~~ — **ทำแล้ว ไม่ผ่าน** → ส่วน B ต้องทำ Worker ก่อน
3. ~~ทำส่วนกลาง~~ — **ทำแล้ว `2026.09.20a`**
4. ~~ทำส่วน B~~ — **ทำแล้ว `2026.09.20b–d`** (ดูข้างล่าง)
5. ที่เหลือคือ **เฟส 2–3 ของส่วน A** (จัดกลุ่ม + เรียบเรียงเป็นตอน) — รอผลข้อ 1 ก่อน

---

# สิ่งที่ทำเพิ่มหลังจากนั้น (20 ก.ย. 2026)

| เวอร์ชัน | ทำอะไร |
|---|---|
| `2026.09.20b` | ปุ่ม **"รวมเป็นอีบุ๊ก · จัดรูป · ไฟล์ EPUB"** ในหน้าจัดการเรื่อง (เดิมแผ่นนี้เปิดได้จากหน้าทริปทางเดียว) · ปุ่มอัปรูปจากเครื่อง · **แก้บั๊ก: ตอนที่ไม่ผูกวันเคยถูกข้ามตอนอ่านรูปที่จัดวางไว้** จัดรูปเสร็จแล้วรูปไม่ขึ้นโดยไม่มีอะไรบอก |
| `2026.09.20c` | **หัวข้อ / ตัวหนา / ยกข้อความ เข้าเล่มและ EPUB แล้ว** — เดิม `_htmlParas()` ลบแท็กทิ้งหมด ทุกท่อนเลยกลายเป็นย่อหน้าธรรมดา · เพิ่ม `_htmlBlocks()` ที่จำชนิดไว้ ⚠ จำนวนท่อนต้องเท่า `_htmlParas()` เสมอ ไม่งั้นรูปที่จัดวางไว้เลื่อนที่ |
| `2026.09.20d` | **เอาเล่มจากเรื่องเล่าออกจากชั้นหนังสือ** — ซ้ำกับแท็บ "เรื่องเล่า" ที่อยู่ข้างกัน และท่วมจอ 71 การ์ด |
