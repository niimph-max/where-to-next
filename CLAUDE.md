# Project: "where to next?" / ทริปหน้าไปไหน

แอปวางแผนทริป + เรื่องเล่า (เจ้าของ = Nim / nimjourney, ผู้ใช้คนแรก)

> **อ่าน "เหตุผลเบื้องหลัง" ก่อนเสนอฟีเจอร์หรือรื้อของเดิม** — หลายอย่างในแอปนี้ดูเหมือนยังไม่เสร็จ
> แต่จริงๆ ถูกตัดสินใจไปแล้วด้วยเหตุผล โค้ดบอกแค่ว่า "ตัดสินใจอะไร" ไม่ได้บอกว่า "ทำไม"

## สำคัญ
- **นี่คือ MOBILE APP เป็นหลัก** — ทุกดีไซน์คงเป็นหน้าจอมือถือเสมอ (เฟรม ~402px, แถบเมนูล่าง 4 อัน: เรื่องเล่า/สำรวจ/แผนของฉัน/ฉัน, ปุ่มขนาดนิ้วแตะ ≥44px)
  ⚠ **มีข้อยกเว้นแล้ว** ตั้งแต่ 22 ก.ย. 69 — ดู "จอคอมบนเว็บ" ข้างล่าง กฎ "ห้ามทำ desktop layout" ใช้กับ**แอป** ไม่ใช่เว็บอีกต่อไป
- **ไฟล์หลักของแอป = `app/index.html`** (แก้ที่นี่) — ไฟล์เดียวจบ ~1.3 MB เสิร์ฟที่ `onevela.net/app`
- `index.html` ที่ root = **หน้าเว็บขาย/landing** (`onevela.net`) คนละไฟล์กับแอป อย่าสับสน
- ⚠ `Where to Next.dc.html` = Design canvas เก่า **ค้างอยู่ที่เวอร์ชัน 2026.07.20z — เลิกใช้แล้ว** เก็บไว้เป็นประวัติเท่านั้น **ห้ามแก้ที่นั่นแล้วก็อปทับ `app/index.html`** จะย้อนแอปกลับไปหลายเดือน
- **ห้ามสร้างโฟลเดอร์ `deploy/` ซ้ำอีก** — เคยมี 2 ชุดแล้วสับสนมาก ลบทิ้งไปแล้ว GitHub Pages เสิร์ฟจาก root
- เวลาแก้เสร็จ: อัปเดต `appVersion` (renderVals) ใน `app/index.html` + `CACHE` ใน `app/sw.js` ให้เป็นเวอร์ชันวันนั้น (รูปแบบ `YYYY.MM.DDx`) **ทั้งสองที่ต้องตรงกัน** ไม่งั้น auto-update ไม่เด้งบิลด์ใหม่
- เปิดหน้าแรกที่ onboarding → เข้า "แผนของฉัน" (planning-first)

---

# เหตุผลเบื้องหลัง — ทำไมถึงเป็นแบบนี้

_บันทึก 20 ก.ย. 2569 จากที่เจ้าของเล่าเอง · ถ้าเจ้าของเปลี่ยนใจให้แก้ที่นี่ด้วย_

## บทบาทของแต่ละรูปแบบ — อันไหนไว้แชร์ อันไหนไว้เก็บ

| รูปแบบ | บนมือถือ | บทบาท |
|---|---|---|
| **ตอนเดียว** (เรื่องเล่า) | อ่านสบาย หน้าเว็บไหลตามจอ | **ตัวหลักสำหรับแชร์** |
| **โฟโต้บุ๊ก** | ดูรูป ไม่ต้องอ่าน | แชร์ได้ เหมาะกับทริปที่รูปเล่าเรื่องเอง |
| **เล่ม A5 ในแอป** | หน้าตายตัว ตัวหนังสือเล็ก | ของสวยไว้พลิกดู **ไม่ใช่ที่นั่งอ่านจริง** |
| **EPUB** | แอปอ่านหนังสือขยายตัวหนังสือได้ | **ทางอ่านจริงจัง** สำหรับเรื่องที่อยากอ่านยาวๆ |

เหตุผล: เจ้าของบอกว่า *"ถ้าให้อ่าน book ของเรา มันลำบากเกินไป เราเองยังไม่อ่าน เพราะหนังสือบนมือถือ ตัวหนังสือเล็ก"*
→ การแชร์จึงเน้นที่**ตอนเดียว** ส่วนใครอยากอ่านเป็นเล่มให้ **เลือกเรื่องที่สนใจแล้วทำ EPUB** ไปอ่านในแอปอ่านหนังสือ

## ลิงก์ = ช่องทางโต · ไฟล์ = ของเก็บ

ลิงก์แชร์คือ**สิ่งเดียวของ Vela ที่คนยังไม่ได้ใช้แอปได้สัมผัส** — เพื่อนเห็นลิงก์ → อาจตามมาโหลดแอป
ส่วนไฟล์ EPUB ไปนั่งอยู่ในแอปอ่านหนังสือแล้วจบ ไม่มีใครรู้จักแอปจากไฟล์ `.epub`
เวลาชั่งน้ำหนักว่าจะลงแรงฝั่งไหน ให้จำข้อนี้ไว้

## ตัดโรงพิมพ์ — ไม่ได้ตัดโฟโต้บุ๊ก (7 ก.ย. 2569)

คอมเมนต์ในโค้ดเขียนว่า *"เลิกสาย PDF/สิ่งพิมพ์ทั้งหมด"* — **อย่าอ่านว่าเลิกทำโฟโต้บุ๊ก**

- ตัดแค่**ช่องทางกระดาษ** เพราะโรงพิมพ์คือธุรกิจที่มีของจริงต้องส่ง มีต้นทุน มีของเสียต้องเคลม ไม่คุ้มจะแบกตอนคนใช้ยังไม่มาก
- **โฟโต้บุ๊กยังอยู่** เปลี่ยนเป็นส่งเป็นลิงก์แทน เพื่อให้โชว์รูปให้เพื่อนดูผ่านแอปได้
- โค้ด `pbPressPrep` (300 dpi + ตัดตก) และหน้า `pbCheck` "ตรวจก่อนสั่งพิมพ์" **ยังอยู่ ไม่มีที่เรียก — อย่าลบทิ้ง** เผื่อวันหลังกลับมาทำ

## แชร์ทริปเป็น PDF (22 ก.ย. 2569)

**ตัดคำว่า "พิมพ์" ออกจากโจทย์แล้ว** เจ้าของบอกชัด: *"ตอนนั้น design คงติดว่าต้องพิมพ์ แต่ที่จริงเราแค่อยากแชร์ pdf สวยๆเท่านั้น"*

ของเดิมติดตรงไหน — `planDocHtml()` ทำเอกสาร A4 ไว้สวยอยู่แล้ว แต่ออกมาเป็นไฟล์ **`.html`**
เพราะ `window.print()` ใน WebView ของ iOS/Android ใช้ไม่ได้ → ผู้ใช้ต้อง เซฟไฟล์ → เปิดในเบราว์เซอร์ → สั่งพิมพ์ → บันทึกเป็น PDF = **4 ขั้น**
และ `exportOverviewPdf()` **ไม่มีปุ่มไหนเรียกเลย** (โค้ดตาย)

**ของใหม่ — `sharePlanPdf()`** ปุ่ม "PDF" ข้างปุ่ม "แชร์" ในหน้าภาพรวมทริป · กดครั้งเดียว → share sheet ของเครื่อง

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `planDocCanvases(D)` | วาดโปรแกรมเดินทางลงหน้ากระดาษ A4 (1240×1754 = 150 dpi) ขึ้นหน้าใหม่เองเมื่อเต็ม |
| `_cvJpegBytes(cv, q)` | canvas → JPEG bytes |
| `_pdfFromJpegs(shots)` | ห่อเป็นไฟล์ PDF ด้วยมือ ไม่ใช้ไลบรารีข้างนอก |
| `_deliverFile()` | ของเดิม — แอป = share sheet · เว็บมือถือ = Web Share · เดสก์ทอป = ดาวน์โหลด |

⚠ **ทำไมวาดเป็นภาพ ไม่ฝังฟอนต์** ตัวสร้าง PDF ฝั่ง JS (jsPDF/pdf-lib) ไม่มีตัวจัดรูปอักษรไทย —
สระบน/ล่าง/วรรณยุกต์จะลอยผิดตำแหน่ง ปล่อยให้เบราว์เซอร์วาดลง canvas แทน ได้หน้าตาตรงกับที่เห็นบนจอ 100%
**ถ้าวันหลังมีคนเสนอให้เปลี่ยนไปฝังฟอนต์เพื่อให้คัดลอกข้อความได้ ให้ทดสอบคำที่มีวรรณยุกต์ก่อนเสมอ** เช่น "ร้านก๋วยเตี๋ยวเรือ"

⚠ **หน่วยความจำ** canvas A4 หนึ่งหน้า ≈ 8.7 MB และระหว่างวาดเก็บไว้ทุกหน้า →
ปล่อยทิ้งทันทีหลังแปลงเป็น JPEG (`p.width = p.height = 0`) และจำกัดที่ `PDF_MAX` = 30 หน้า

⚠ **ฟอนต์โหลดแบบขี้เกียจ** ต้อง `document.fonts.load()` ก่อนวาด ไม่งั้นหัวเรื่องจะไม่ใช่ Chonburi

### ⚠⚠ เรื่องรูป — ข้อที่เคยพังมาแล้ว เจ้าของเตือนเอง

*"ให้ระวังเรื่องรูปด้วย ตอนนั้นรูปไม่มา"*

### หลักที่เจ้าของวางไว้ — รูปของ "แผน" ไม่ใช่รูปของ "ทริปที่ไปแล้ว"

*"เรื่องรูปควรเลือกจากรูปที่เราจะเอามาใส่ไว้ตรงนี้ เพราะปกติแล้วรูปจะเอามาจากเน็ต ไม่ใช่รูปจากการเที่ยวจริง เพราะแผนเที่ยวจะส่งก่อนเที่ยว"*
(ชี้ที่ **รูปปกทริป** หัวหน้าจอทริป = `<photo-pick id="wtn-cover-<trip>">`)

**รูปปกทริปคือรูปหลักของเอกสารนี้ ไม่ใช่รูปสำรอง** — PDF ถูกส่งไป**ก่อน**เดินทาง แปลว่ายังไม่มีเช็กอิน ไม่มีโมเมนต์ ไม่มีรูปถ่ายจริงสักใบ
ปกที่เจ้าของหามาจากเน็ตจึงเป็นรูปเดียวที่มีอยู่ในเอกสารตามปกติ → ให้ timeout นานกว่าใบอื่น (18 วิ) และแยกนับไว้ต่างหาก
ถ้าปกโหลดไม่ขึ้นจะเด้งบอกเจาะจงว่า **"รูปปกทริปโหลดไม่ขึ้น"** ไม่ใช่รวมไปกับ "มีรูป N ใบโหลดไม่ขึ้น"

รูปรายวันจากเช็กอิน **ยังอยู่ แต่เป็นของแถมสำหรับทริปที่ไปแล้ว** — แผนที่ยังไม่ได้ไปจะไม่มี และนั่นถูกต้อง ไม่ต้องเตือน
⚠ ในแผนไม่มีช่องใส่รูปรายวัน/รูปต่อสถานที่เลย — **ถ้าจะเพิ่ม ต้องถามเจ้าของก่อน อย่าคิดเอง**

**สาเหตุของเดิม** `planDocData()` ดึงปกจากช่องเดียว — `photopick:wtn-cover-<trip>` ซึ่งมีค่า**ต่อเมื่อผู้ใช้กด "เลือกปก" เอง**
คนส่วนใหญ่ไม่เคยกด → `cover = ''` → เอกสารออกมาไม่มีรูปสักใบ ทั้งที่ในทริปมีรูปเช็กอินเต็มไปหมด
และของเดิมไม่มีรูปรายวันเลยด้วย มีแค่ปกใบเดียว

**แก้แล้ว 4 ชั้น** (ทุกข้อทดสอบใน Chromium แล้ว)

| กับดัก | ทางกัน |
|---|---|
| ไม่เคยเลือกปก | `planDocPhotos()` ไล่ `wtn-cover-` (ช่องที่เจ้าของใส่รูปจากเน็ต) → `wtn-pb-cover-` → `wtn-eb-cover-` → รูปเช็กอินใบแรก |
| ไม่มีรูปรายวัน | ดึงรูปจากเช็กอินของวันนั้น วันละไม่เกิน 3 ใบ (ข้าม `spriteOf`) |
| รูปโหลดค้าง | `_imgCap()` timeout 9 วิ/ใบ (ปก 14 วิ) + งบเวลารวม 45 วิ — ของเดิม `_img` ไม่มี timeout เลย **แอปค้างที่ "กำลังทำ PDF…" ตลอดกาลได้** |
| รูปโหลดไม่ขึ้นแล้วเงียบ | นับ `want/got` แล้วเด้งทอสต์ "มีรูป N ใบโหลดไม่ขึ้น" ก่อนส่งไฟล์ — **ห้ามเงียบ** คนจะไม่รู้ว่าไฟล์ที่ส่งไปให้เพื่อนรูปหาย |

⚠ **canvas เปื้อน (tainted)** — `loadImgForCanvas()` ของเดิมจัดการให้แล้ว: ลอง crossOrigin → fetch blob → พร็อกซี 3 เจ้า
และถ้ายังเปื้อนจะคืน `null` **ห้ามข้ามฟังก์ชันนี้ไปใช้ `new Image()` ตรงๆ** เพราะรูปเก่าบน Firebase ไม่ส่ง CORS header
ถ้าเปื้อนแล้ว `toBlob()` จะโยน SecurityError = **ทั้งไฟล์พัง ไม่ใช่แค่รูปใบเดียวหาย**

### ⚠ บนคอมห้ามใช้ Web Share — เจอจากการใช้จริง (22 ก.ย. 2569)

เจ้าของรายงาน: *"ตอนกด copy เพื่อจะไปวาง แล้วไม่มีไฟล์ติดมา"* และ *"แต่ส่งจากมือถือได้"*

Chrome บน Windows ตอบ `navigator.canShare({files})` = **true** (ยืนยันในเครื่องเจ้าของแล้ว) โค้ดเลยเรียก `navigator.share()`
แล้วเปิด **แผงแชร์ของ Windows** ขึ้นมา — แต่ปุ่ม "คัดลอก" ในแผงนั้นคัดลอก ***ลิงก์*** ไม่ใช่ไฟล์
ผู้ใช้ไปวางใน LINE/แชตจึงไม่มีอะไรติดไป และแย่กว่านั้นคือ **เงียบ** ไม่มี error อะไรเลย

**แก้: `_deliverFile()` ใช้ Web Share เฉพาะมือถือจริง** (`isTouchMobile` — เช็ก UA ไม่ใช่ `maxTouchPoints`
เพราะโน้ตบุ๊กจอสัมผัสของ Windows ก็ได้ค่ามากกว่า 0) บนคอมดาวน์โหลดตรงๆ ได้ไฟล์จริงไว้แนบเอง

⚠ `_deliverFile()` ใช้ร่วมกับไฟล์สำรอง · การ์ดโมเมนต์ · คลิปรีแคป ด้วย — การแก้นี้ทำให้ทุกอันบนคอมดาวน์โหลดเหมือนกันหมด ซึ่งถูกแล้ว
**อย่าเอา Web Share กลับมาใช้บนเดสก์ทอปอีก** ต่อให้ `canShare` จะตอบ true ก็ตาม

⚠ **ทริปที่ยังไม่ได้ไป ไม่มีรูปเป็นเรื่องปกติ** (ยังไม่มีเช็กอิน) — ไม่ต้องเด้งเตือน เอกสารข้อความล้วนถูกแล้ว

ทดสอบแล้ว (Chromium, 22 ก.ย. 69) ผ่าน `qpdf --check` ทุกกรณี:
ทริป 5 วัน 13 กิจกรรม 12 รูป → 4 หน้า 428 KB · ทริป 20 วัน 60 กิจกรรม → 13 หน้า 2.9 MB · ทริปเปล่า/ภาษาอังกฤษ → 1 หน้า ·
เคสรูปพัง (โหลดไม่ขึ้น + ค้างตลอดกาล) → ไฟล์ออกปกติใน 10 วิ ข้ามแถวรูปที่ว่าง และเด้งทอสต์บอกจำนวนที่หาย

**ยังไม่ได้ทำ** ป้าย "(ต่อ)" ตอนวันเดียวถูกแบ่งข้ามหน้า — ยังไม่เจอว่าจำเป็น

## ⚠⚠ src="{{ }}" คือคำขอจริง — ทำหน้าเว็บค้าง 221 วินาที (22 ก.ย. 2569)

เจ้าของแจ้ง *"น่าจะมีปัญหา ไม่เคยเปิดช้าแบบนี้"* หน้าขาวเปล่า เปิดไม่ขึ้นเลย

**สิ่งที่วัดได้จากเครื่องเจ้าของ** (ตอนหน้าค้างอยู่ 221 วินาที `document.readyState` ยังเป็น `loading`)

| ตัวเลข | ค่า |
|---|---|
| คำขอปลอมจากเทมเพลต (`%7B%7B...%7D%7D`) | **38 ใบ** |
| ใบที่ช้าสุด | **107 วินาที** |
| `icon-192.png` (ไฟล์จริง) | 106 วินาที |
| รูปทริปของเจ้าของ | 48–81 วินาที |
| เน็ต/เซิร์ฟเวอร์ (วัดแยก) | **ปกติ — ตอบใน 1 วินาที** |

**กลไก** `.dc.html` เขียน `src="{{ frSrc }}"` ไว้ในแท็ก `<img>` — เบราว์เซอร์ยิงออกไปเป็น URL จริง
**ก่อน**ที่คอมโพเนนต์จะแทนค่า ในไฟล์มีแบบนี้ **48 จุด** ทุกใบ 404 แต่ยังกินคอนเนกชัน
และ service worker รอถึง 8 วิต่อใบ (`fromNet(req, 8000)`) พอแคชว่าง (เพิ่งขึ้นเวอร์ชันใหม่)
ของพวกนี้วิ่งออกเน็ตพร้อมกันไปแย่งคิวกับสคริปต์/รูปจริงจนหน้าโหลดไม่เสร็จ

⚠ **ทำไมเพิ่งเจอ** ปัญหาอยู่มานานแล้ว แต่ปกติของพวกนี้อยู่ในแคช · วันนั้นปล่อย 4 เวอร์ชันรวด (22c→22f)
แคชถูกล้างทุกรอบ → **ยิ่งปล่อยเวอร์ชันถี่ ยิ่งเจอ** เป็นผลข้างเคียงของการ deploy บ่อย ไม่ใช่โค้ดที่แก้ในรอบนั้น

**แก้แล้วใน `sw.js`** ตัดคำขอที่มี `{{` / `%7B%7B` ทิ้งทันทีที่ต้นทาง ไม่ออกเน็ต ไม่แตะแคช

ผลหลังแก้ (วัดในเครื่องเจ้าของ): คำขอปลอมเหลือ **4 มิลลิวินาที** ต่อใบ · ไฟล์จริงช้าสุด 7 มิลลิวินาที ·
**เปิดแอป (แคชอุ่น) DOMContentLoaded 260 ms · โหลดครบ 1.2 วินาที** จากเดิม 221 วินาทีแล้วยังไม่เสร็จ

### แก้ที่ต้นเหตุแล้ว — ห่อมาร์กอัปไว้ใน `<template>` (22n — 22 ก.ย. 2569)

เจ้าของขอให้จัดการต้นเหตุให้จบก่อนขึ้นแอป iOS **ไม่ต้องไล่แก้ทีละจุดใน 48 จุด** —
ห่อมาร์กอัปทั้งก้อนไว้ในแท็ก `<template data-dc-tpl>` แค่ที่เดียว

เนื้อในแท็ก `<template>` ถูกแตกเป็น **DocumentFragment ที่ไม่ทำงาน** — ไม่โหลดรูป ไม่โหลด iframe ไม่รันสคริปต์
แล้ว `support.js` คลี่ห่อออกก่อนเรนเดอร์ (`dcTemplateHtml()` ฝั่ง DOM · regex ฝั่งข้อความใน `parseDcText`)

| | ก่อน | หลัง |
|---|---|---|
| คำขอปลอม (`%7B%7B`) | **41 ใบ** | **0 ใบ** |
| คำขอทั้งหมดตอนเปิด | 121 | **72** |
| `<img>` จริงในเอกสารตอนแตกหน้า | 46 | **0** |
| DOMContentLoaded | — | 207 ms · โหลดครบ 582 ms |

⚠⚠ **`<helmet>` ต้องอยู่นอกห่อเสมอ** ข้างในมี `<link>` ฟอนต์ · `supabase-config.js` · `wtn-backend.js` ·
สคริปต์ลงทะเบียน service worker — ถ้าโดนห่อด้วย **ทั้งหมดจะไม่ทำงาน**

**ตาข่ายนิรภัย** `index.html` กับ `support.js` เป็นคนละไฟล์ Cloudflare แคชแยกกัน (4 ชม. สำหรับ .js)
จึงมีช่วงที่เบราว์เซอร์ได้ index ใหม่คู่กับ support เก่า → เอนจินเก่าจะเรนเดอร์แท็ก template ทั้งก้อน = **หน้าขาว**
กันด้วยธง `window.__dcTplAware` (support.js ปัก) + สคริปต์ท้าย `</x-dc>` ที่คลี่ห่อเองถ้าไม่เจอธง
**ยอมกลับไปยิงคำขอปลอมแบบเดิม ดีกว่าหน้าขาว**

⚠ **อย่าเอา guard ใน `sw.js` ออก** ถึงต้นเหตุจะแก้แล้ว แต่ในแอป native ไม่มี service worker
และไฟล์ `.dc.html` อื่นๆ ยังไม่ได้ห่อ — guard ยังต้องอยู่

ทดสอบแล้ว (22 ก.ย. 69): คลี่ห่อได้สตริงเทมเพลตเดิม **เท่ากันทุกตัวอักษร 412,493 ตัว** (ทดสอบทั้งฝั่ง DOM และฝั่งข้อความ) ·
ไฟล์ที่ไม่ได้ห่อยังทำงานเหมือนเดิม · ตาข่ายนิรภัยคลี่แล้วได้ DOM เดิมเป๊ะ (46 `<img>` เท่าเดิม) · วัดผลจริงบนเว็บที่ปล่อยแล้ว

## ⚠⚠ ลบกิจกรรมในแผนแล้วเด้งกลับมา — baseline ไม่ใช่คำสั่งลบ (22 ก.ย. 2569)

เจ้าของลบกิจกรรมที่ซ้ำออกจากวันแรกของทริปอิตาลีใต้ แล้วมันกลับมาเองทุกรอบ
*"เด้งกลับมาอีกแล้ว นี่ไงคือปัญหา ลบไปแล้วกลับมาอีก"*

**ปุ่มลบทำงานถูกต้องตลอด** — วัดในเครื่องเจ้าของแล้ว: 4 → 3 ใบทั้งในเครื่องและบนคลาวด์
**สิ่งที่พังคือการซิงก์** `_mergeDay()` มีบรรทัดนี้ `const stops = [...ld.stops]` —
**ของในเครื่องตัวเองถูกเก็บไว้ทุกใบเสมอ ไม่มีทางถูกเอาออก** แปลว่า **การลบข้ามเครื่องไม่ได้เลย**

| ลำดับ | เกิดอะไร |
|---|---|
| 1 | เครื่อง A ลบใบที่ 2 → ดันขึ้นคลาวด์ เหลือ 3 ใบ · baseline ของ A เดินตามเป็น 3 |
| 2 | มือถือ B (ยังไม่ได้ซิงก์ ยังมี 4 ใบ) ซิงก์ทีหลัง — merge เก็บของตัวเองครบ 4 → **ดันกลับขึ้นคลาวด์เป็น 4** |
| 3 | A ดึงลงมา เจอใบที่ตัวเองลบไปแล้ว · baseline ของ A **ไม่มีใบนั้นแล้ว** (เดินผ่านไปตั้งแต่ข้อ 1) |
| 4 | กฎเดิมอ่านว่า "ไม่อยู่ใน base + ไม่อยู่ในเครื่อง = ของใหม่จากอีกเครื่อง" → **ใส่กลับ** วนไม่จบ |

⚠⚠ **บทเรียน: baseline บอกได้แค่ "เครื่องนี้เคยเห็นอะไร" มันไม่เคยบอกว่า "ใครตั้งใจลบ"**
พอ baseline เดินผ่านจุดที่ลบไปแล้ว หลักฐานการลบก็หายไปด้วย — **การลบต้องมีใบสั่งลบเสมอ**
(โมเมนต์รู้เรื่องนี้มานานแล้ว `wtn-tombs` · แผนรายวันตกหล่นไป)

**แก้แล้ว: `wtn-stop-tombs`** คีย์ `'<tripId>:<ดัชนีวัน>:<kind|title|time|period>'` → เวลาที่ลบ · เก็บ 90 วัน

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `_addSTomb()` | `delStop()` จด**ก่อน**ตัดออก (อ่าน `stops[si]` ก่อน `splice` ไม่งั้นได้ของผิดใบ) |
| `_mergeDay(…, pre)` | กรอง **ทั้งสองฝั่ง** — ฝั่งอีกเครื่องกันเครื่องรุ่นเก่าดันกลับ · ฝั่งตัวเองทำให้การลบข้ามเครื่องได้จริง |
| `_mSTombs` | `_mergeData()` รวมใบสั่งลบ local+remote ก่อน merge (แบบเดียวกับ `_mTombs`) |
| `_mergeVal` | `wtn-stop-tombs` อยู่สาขาเดียวกับ `wtn-tombs` — **รวมกัน ห้ามทับกัน** |
| `_sweepSTombs()` | ใส่กิจกรรมคีย์เดิมกลับ = ยกเลิกใบสั่งลบ · เรียกจาก `saveItinerary()` **จุดเดียว** เพราะทุกทางที่เพิ่มกิจกรรม (เพิ่มเอง/นำเข้าแผน/ผู้ช่วยจัดวัน) ผ่านตรงนั้นหมด |

⚠ **ใบสั่งลบต้องอยู่ใน SKIP ของการกู้ไฟล์สำรอง** (และถูกล้างหลังกู้) เหมือน `wtn-tombs` —
ไม่งั้นกู้ข้ามบัญชีแล้วใบสั่งลบของบัญชีเก่าจะตามมาลบของที่เพิ่งกู้

⚠ **iOS/Android ยังเป็น 1.0.6 (22f)** — เครื่องพวกนั้นยังดันใบที่ลบแล้วขึ้นคลาวด์ซ้ำได้เรื่อยๆ
บนเว็บจะกรองทิ้งทุกรอบ **หน้าจอเจ้าของจึงสะอาด** แต่จะสะอาดทุกเครื่องก็ต่อเมื่อออก native 1.0.7

ทดสอบแล้ว (Node, 22 ก.ย. 69 — ดึงเมธอดจริงจากไฟล์ที่ปล่อย ไม่ได้พิมพ์ซ้ำ) 14 ข้อผ่านหมด
รวมข้อที่**จำลองบั๊กเดิมให้เห็นว่าเด้งกลับจริง** · ลบแล้วข้ามเครื่องได้ · ของที่ไม่ได้ลบไม่หาย ·
เพิ่มกลับแล้วอยู่ต่อได้ · แยกทริป/แยกวันถูกต้อง · หมดอายุ 90 วัน

## ⚠⚠ ตาราง "งานที่ทำไปแล้ว" ถูกซิงก์ทับจนงานหาย (22 ก.ย. 2569)

**อาการ** ทำรูปย่อครบ 434 รูปบนเดสก์ท็อป ตรวจแล้วเรียบร้อย (แผนที่ 434 · สำเร็จ 434 · เหลือ 0 · บนคลาวด์ก็ครบ 434)
แล้วต่อมา **กล่อง "ดูแลรูปภาพ" กลับมาขึ้นเองทั้งที่ไม่ได้ทำอะไรเลย** · iOS ขึ้นเหลือ "ร้อยกว่ารูป"

**สาเหตุ — `_mergeVal()` บรรทัดนี้**

```js
if (ls === bs) return rs;   // "ฉันไม่ได้แก้คีย์นี้ → เอาของอีกเครื่อง"
```

ถูกต้องสำหรับค่าที่คนแก้ แต่**ผิดมหันต์กับตารางบันทึกงานที่ทำแล้ว**

1. เดสก์ท็อปทำครบ 434 แล้วดันขึ้นคลาวด์ · `wtn-sync-base` = 434
2. iOS ดึงภาพ**กลางคัน** (~300 รูป เพราะ push เป็นระยะทุก 12 วิ) แล้วดันกลับขึ้นคลาวด์
3. เดสก์ท็อปซิงก์: `ls`(434) `=== bs`(434) → **รับ `rs`(300) มาทับ** → งานที่ทำเสร็จหายไป 134 รูป
4. กล่องเด้งกลับ · และจะ**ปิงปองกันไปมาไม่จบ**ระหว่างเครื่อง

**แก้แล้ว** `wtn-thumb-map` และ `wtn-rehost-map` ใช้ **union** เหมือน `wtn-tombs`
ตารางพวกนี้เป็นบันทึก "เคยทำแล้ว" ต่อรายการ ไม่ใช่ค่าที่คนแก้ → รวมกันเสมอ **ไม่มีวันเสียของ**
และให้ `1` (สำเร็จ) ชนะ `'x'` (ล้มเหลว) ไม่งั้นรูปที่ทำได้แล้วจะถูกตีตราว่าเสียตลอดไป

⚠ **กฎที่ควรจำ** คีย์ไหนก็ตามที่เป็น "บันทึกว่าทำอะไรไปแล้วบ้าง" (ledger) **ต้อง union**
ห้ามปล่อยให้ตกไปใช้กฎรวมข้อมูลทั่วไป เพราะกฎนั้นออกแบบมาสำหรับ "ค่าที่คนแก้" ไม่ใช่ "งานที่เครื่องทำ"

ทดสอบแล้ว (Node): local 434 + base 434 + remote 300 → **ได้ 434** · กลับด้านก็ได้ 434 ·
ฝั่งใดฝั่งหนึ่งยังไม่มีตาราง → ได้ 434 · `1` ชนะ `'x'` · คีย์อื่นยังใช้กฎเดิมไม่เปลี่ยน

⚠ **บทเรียนของผมเอง** รอบนี้ผมบอกเจ้าของไปว่า "กดซิงก์แล้วจะหาย" โดยดูแค่ว่าข้อมูลบนคลาวด์ครบ
แต่ไม่ได้ไล่กฎ merge ว่าฝั่งรับจะทำอะไรกับมัน — **ครบบนคลาวด์ ไม่ได้แปลว่าปลอดภัย**

### ⚠⚠ ภาคสอง: รวมถูกแล้ว แต่ไม่เคยเขียนลงเครื่อง (22k — 22 ก.ย. 2569)

22i แก้ `_mergeVal` ให้ "รวม" ตารางถูกต้องแล้วจริง **แต่ยังไม่จบ** เพราะ `_writeLocal()` มี

```js
const keep = { …, 'wtn-thumb-map': 1, 'wtn-rehost-map': 1 };   // = ห้ามเขียนทับของในเครื่อง
Object.keys(map).forEach(k => { if (keep[k]) return; this._setLS(k, map[k]); });
```

ค่าที่รวมแล้วจึง**ถูกดันขึ้นคลาวด์อย่างเดียว ไม่เคยลงเครื่องตัวเอง** —
เครื่องที่ตารางไม่ครบ **ซิงก์กี่รอบก็ไม่มีวันครบ** ต่อให้คลาวด์ถูกต้อง 100%

**ทำไมกว่าจะเจอนานขนาดนี้** เดสก์ท็อปเป็นคนทำงานย่อรูปเอง ตารางเลยครบตั้งแต่แรก **ไม่มีอาการ**
อาการโผล่เฉพาะ "เครื่องที่ไม่ได้ทำงานนั้น" — ผมไล่หาสาเหตุจากเครื่องที่ไม่มีอาการอยู่หลายรอบ

ตัวเลขที่ชี้ขาด: คลาวด์ 434 · คอม 434 (ขึ้น 0 รูป) · **มือถือขึ้น 298** → 434 − 298 = 136 = ตารางของมือถือ

**แก้แล้ว** แยก `keep` (ห้ามเขียน) ออกจาก `noDel` (ห้ามลบ) — ตารางทั้งสองอยู่ใน `noDel` อย่างเดียว
เขียนได้อย่างปลอดภัยเพราะ `_mergeVal` คืน superset ของในเครื่องเสมอ (`Object.assign({}, remote, local)`)

⚠ **เพิ่มเลขให้ดูในกล่องแล้ว** — "(ตารางที่ทำแล้วในเครื่องนี้ N รายการ)" เพราะเรื่องนี้เดาผิดมาสามรอบ
เหตุผลเดียว: **ไม่มีเลขให้ดูจากมือถือ** เลยได้แต่เดาจากเครื่องที่ไม่มีอาการ **อย่าเอาบรรทัดนี้ออก**

⚠⚠ **กฎที่ควรจำ (ภาคสอง)** ทุกครั้งที่แก้ `_mergeVal` ให้เพิ่มคีย์ใหม่ **ต้องไปดู `keep` ใน `_writeLocal` ด้วยเสมอ**
merge ถูกอย่างเดียวไม่พอ ถ้าปลายทางไม่รับเขียน มันคือการแก้ที่ไม่มีผลกับเครื่องที่มีอาการเลย

## กล่อง "ดูแลรูปภาพ" เด้งกลับมาไม่รู้จบ — อาการต่อเนื่องจากข้อบน (22 ก.ย. 2569)

เจ้าของ: *"ขึ้นมาให้ทำบ่อยมาก เรากดสร้างรูปย่อ แล้วก็มาอีก"*

**หลักฐานชี้ขาด** `wtn-thumb-map` ใน localStorage **ว่างเปล่า 0 ไบต์** ทั้งที่กดมาหลายครั้ง
แปลว่า `backfillThumbs()` **ไม่จบแม้แต่รอบเดียว** (แผนที่บันทึกทีละรูป ถ้าทำได้สักรูปต้องมีข้อมูล)

**สาเหตุ** `loadImgForCanvas()` ไม่มี timeout เลย · ตอนคำขอปลอม 38 ใบกินคิวอยู่ รูป**ใบแรก**ค้าง → ลูปไม่เดิน → แผนที่ว่าง → กล่องเด้งกลับทุกครั้ง
ซ้ำร้าย `rehostLegacyImages()` ขึ้นทอสต์ **"✓ รูปย่อครบแล้ว" ทุกครั้งแม้ไม่ได้ทำอะไรเลย** เจ้าของเลยเห็นว่าสำเร็จแต่กล่องยังอยู่

**พิสูจน์ว่าท่อไม่ได้พัง** — ยิงจริง 1 รูปในเครื่องเจ้าของหลังแก้ sw.js: โหลด 103 ms · ย่อได้ 45 KB · `uploadThumbFor` สำเร็จใน **1.6 วินาที**

**แก้แล้ว**

- `backfillThumbs()` ใส่ `_imgCap()` — โหลดรูป 12 วิ · อัปโหลด 20 วิ **รูปเดียวค้างต้องไม่ทำให้ทั้งงานค้าง**
- คืนค่า `{done, failed, total}` แล้วทอสต์บอกตัวเลขจริง **ห้ามบอก "ครบแล้ว" ลอยๆ อีก**
- ข้อความในกล่องบอกเวลาที่ต้องใช้ (434 รูป ≈ 12 นาที) และบอกว่าหลุดกลางทางกดซ้ำได้ ระบบทำต่อจากเดิม

⚠ งานนี้ทำทีละรูปแบบ sequential (~1.7 วิ/รูป) **ห้ามยิงขนานเยอะๆ** — Supabase egress และโควตาอัปโหลดจะโดนกระแทก

## ยืดวันเดินทางแล้ววันไม่เพิ่ม — สามบั๊กในจุดเดียว (22 ก.ย. 2569)

เจ้าของขยายวันสุดท้ายของทริปอิตาลีเป็น 17 เม.ย. แต่แถบวันยังมีแค่ 7 วัน
*"เราเพิ่มวัน ให้ถึงวันที่ 17/4 แต่ทำไม วันที่ ไม่เพิ่มตามด้วยล่ะ"*

| อาการ | สาเหตุ | แก้ |
|---|---|---|
| ขยายช่วงวันแล้ววันไม่เพิ่ม | `baseDaysFor()` คืนแผนที่บันทึกไว้ทั้งก้อน — `userDays()` ที่คิดจาก start/end ถูกใช้**เฉพาะตอนยังไม่มีแผนบันทึก** จำนวนวันจึงค้างตั้งแต่วันสร้างทริป | `padDays()` เติมวันท้ายให้ครบตามช่วงวันเดินทาง |
| หัวข้อวันโชว์วันที่เก่า ("วันที่ 1 · 8 ม.ค." ทั้งที่ชิปบอก 7 เม.ย.) | `tag` ถูกเขียนตายไว้ตอน `importVelaPlan` และ `withRealDates()` คำนวณใหม่แค่ `date` ไม่แตะ `tag` | `withRealDates()` คำนวณ `tag` ใหม่ด้วย |
| ปุ่ม **+** ในแถบวันกดแล้วไม่เกิดอะไร | ปุ่มนั้น**ไม่มี `onClick` เลย** มาตั้งแต่ต้น | `addTripDay()` |

⚠⚠ **`padDays()` เติมท้ายอย่างเดียว ห้ามตัดทิ้งเด็ดขาด**
วันที่มีอยู่มีกิจกรรม/ที่พักผูกอยู่ และเช็กอิน/โมเมนต์คีย์ด้วย `'<tripId>:<index>'` —
ถ้าตัดวันตามช่วงที่ย่นลง ของพวกนั้นจะกลายเป็นขยะที่หาไม่เจอ **ย่นวันเดินทางจึงไม่ลบวันให้ ต้องให้คนตัดสินใจเอง**

**`addTripDay()` เลื่อน `end` ของทริป ไม่ใช่ต่อวันลงแผนตรงๆ** เพื่อให้ "ช่วงวันเดินทาง" กับ "จำนวนวัน" เป็นความจริงอันเดียวกัน
(ถ้าต่อลงแผนเฉยๆ หัวทริปจะบอก 7 วัน แต่แถบวันมี 8 — คนละความจริงสองอัน) · ทริปที่ยังไม่ตั้งวันถึงจะต่อลงแผนตรงๆ · เพดาน 90 วันตาม `dayCount()`

ทดสอบแล้ว (Node, 22 ก.ย. 69): 7 วัน + ช่วง 11 วัน → ได้ 11 วัน · ชื่อวันคำนวณใหม่ถูกทั้งวันแรก/วันสุดท้าย ·
กิจกรรมเดิมไม่หาย · ย่นช่วงเหลือ 5 วันแล้วยังเป็น 11 (ไม่ตัด) · ทริปไม่ตั้งวันไม่ถูกแตะ

## เลือกตอนที่ลงเล่ม EPUB (22 ก.ย. 2569)

เจ้าของขอ: *"ในเรื่องเล่า สามารถเลือกตอนที่ลง epub ได้"* — บางตอนไม่อยากให้อยู่ในหนังสือ

**ก่อนหน้านี้ทุกตอนที่มีเนื้อลงเล่มหมด ไม่มีทางเลือก** (โมเมนต์มีสวิตช์ `inBook` มานานแล้ว แต่ตอนไม่มี — เป็นช่องที่ตกหล่น ไม่ใช่การตัดสินใจ)

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `chInBook(c)` | `!(c.inBook === false)` — **ค่าเริ่มต้นคือลง** เช็ค `=== false` ไม่ใช่ falsy เพื่อให้เล่มเดิมหน้าตาไม่เปลี่ยน |
| `ebBookMap()` | แผนที่ตอน**เฉพาะที่จะลงเล่ม** ใช้ใน `ebGather()` และ `ebEstimate()` |
| `ebChapterMap()` | ตัวเต็ม **ห้ามกรอง** — หน้าจัดรูปในเล่ม/แก้ไขต้องเห็นทุกตอน ไม่งั้นตอนที่ปิดไว้จะแก้ไม่ได้ |
| สวิตช์ | "ใส่ในเล่ม EPUB" ในรายชื่อตอนของเรื่องเล่า แถวล่างสุดของการ์ดแต่ละตอน |

**พฤติกรรมที่ตั้งใจ** ปิดตอนของวันไหน → วันนั้น**ยังอยู่**ในเล่มถ้ามีโมเมนต์ที่ติ๊กลงหนังสือไว้
จะหายทั้งวันก็ต่อเมื่อวันนั้นไม่มีโมเมนต์เลย ("ไม่อยากให้เรื่องที่เขียนอยู่ในเล่ม" ≠ "อยากลบวันนั้นทิ้ง")

⚠ **โฟโต้บุ๊กไม่เกี่ยว** `pbGather()` / `pbShotCount()` ยังใช้ `ebChapterMap()` ถูกแล้ว — มันใช้รูปจากโมเมนต์ ไม่ได้ใช้เนื้อเรื่อง

⚠ **สวิตช์ต้องแตะ `_m: Date.now()` ด้วย** ตัวรวมข้อมูลสองเครื่องเลือกตอนด้วยเวลาแก้ล่าสุด (`updatedAt`/`createdAt`/`_m`)
ถ้าไม่แตะ สวิตช์ที่เพิ่งกดจะถูกฉบับของอีกเครื่องทับกลับ**เงียบๆ** ตอนซิงก์ — `toggleFree` เคยมีบั๊กนี้ แก้ไปพร้อมกันแล้ว
**ไม่แตะ `updatedAt`** เพราะบรรทัด "แก้ไขเมื่อ…" ในรายชื่อตอนใช้ค่านั้น จะกลายเป็นโกหก

## โฟโต้บุ๊ก: เพิ่มรูปจากเครื่องเข้าเล่มได้ + สองแหล่งที่ตอบคนละอย่าง (23 ก.ย. 2569)

เจ้าของ: *"รูปถ่ายด้วยกล้องใหญ่ ยังไม่ได้ใส่เข้ามาตอนใช้มือถือ · ตอนปรับกรอบรูป เห็นรูปที่เข้ากับหน้านี้ ก็จะดึงไปใส่เลย"*

### บั๊กที่เจอก่อน — หน้าเดียวกันขึ้น "198 รูป" กับ "ทริปนี้ยังไม่มีรูป" พร้อมกัน

| การ์ดบนหน้า "สรุปทริป" | ยึดอะไร |
|---|---|
| รีแคป/คลิป | `this.state.trip` — ทริปที่เปิดอยู่ |
| ทำเป็นโฟโต้บุ๊ก (ของเดิม) | `ebTripId()` — **เรื่องเล่าที่เปิดค้างอยู่** |

`ebTripId()` ออกแบบมาเพื่อเล่ม EPUB จึงยึดเรื่องเล่า และ**คืนค่าว่างถ้าเรื่องนั้นไม่ผูกทริป** (ตั้งใจ ห้ามแก้ที่ตัวมัน)
เจ้าของมีเรื่องนำเข้าจาก WordPress **69 เรื่องที่ไม่ผูกทริป** พอเปิดอ่านแล้วเด้งมาหน้าสรุปทริป โฟโต้บุ๊กเลยมองไม่เห็นทริป

**แก้ด้วย `pbTripId()`** — บนหน้า `summary` ยึด `state.trip` เสมอ · เปิดจากชั้นหนังสือ (`_ebStoryOverride`/`_ebTripOverride`) ยังเคารพของเดิม
⚠ `pbGather()` ต้องใช้ `pbTripId()` ตัวเดียวกัน ไม่งั้นการ์ดบอกมีรูป แต่สร้างเล่มออกมาว่าง

**และ `pbShotCount()` เคยบวกรูปของเรื่องเล่าเข้ามา** ทั้งที่ `pbGather()` ข้ามรูปพวกนั้นโดยตั้งใจ (รูปเดิมอัปซ้ำ จะซ้ำในเล่ม)
การ์ดเลยบอก 137 แต่เล่มจริงได้ 126 — **ตัวเลขบนการ์ดต้องตรงกับเล่มเสมอ** ตัดออกแล้ว

### ฟีเจอร์: เพิ่มรูปจากเครื่อง (`wtn-pb-extra`)

ปุ่ม **+** ที่หัวแต่ละหน้าในจอ "ปรับกรอบรูปในเล่ม" → เข้า**วันของหน้านั้น**ทันที (เจ้าของเลือกแบบนี้เอง ไม่ต้องเลือกวัน)

- คีย์ `'<tripId>:<ดัชนีวัน>'` → อาร์เรย์ของ ref · ย่อที่ 2600px q0.9 (ใหญ่กว่า EPUB เพราะเล่มเอาไปพิมพ์)
- เก็บด้วย `stashPhoto()` เป็น `'idb:p...'` — **ห้ามเก็บ data url ลง localStorage** พื้นที่หลักจำกัดราว 5 MB รูปกล้องใหญ่ใบเดียวก็เต็ม
- `pbGather()` ต่อท้ายรูปจากโมเมนต์ · ลำดับ/กรอบ/ขนาด คีย์ด้วย ref อยู่แล้ว จึงลากจัดได้เหมือนรูปอื่น **ไม่ต้องเขียนเพิ่ม**
- **ไม่กลายเป็นโมเมนต์ ไม่ขึ้นฟีด** — ของเดิม (รูปจากโมเมนต์) ยังเป็นฐานเหมือนเดิม ตามที่เจ้าของขอ

⚠⚠ **รูปพวกนี้ขึ้นคลาวด์เองอัตโนมัติ ไม่ต้องเขียนโค้ดอัปเลย**
`hoistImages()` กวาด**ทุกคีย์ `wtn-*`** หา ref `idb:p...` แล้วอัปขึ้น R2 พร้อมเขียนทับ ref เป็นลิงก์ให้
(ทดลองจริงแล้ว: หลังเพิ่มรูป ค่าที่เก็บกลายเป็นลิงก์ https ทันที)
**ผลดี** รูปที่เพิ่มข้ามเครื่องได้จริง · **ผลเสีย** กินพื้นที่เก็บและโควตาอัปโหลดตามจำนวนรูป
⚠ **อย่าเขียนโค้ดอัปเองซ้ำอีกชั้น** เดี๋ยวอัปสองรอบ · และคีย์ใหม่ใดๆ ที่ชื่อขึ้นต้น `wtn-` จะโดนกวาดแบบนี้เสมอ

### ลบ / เปลี่ยนรูปในเล่ม (23c)

แตะรูปในหน้าปรับกรอบ → มีปุ่ม **เปลี่ยนรูปนี้** กับ **เอาออกจากเล่ม**

- **เอาออก = ไม่เอาเข้าเล่มเท่านั้น** เก็บใน `wtn-pb-off` แยกต่างหาก · **ไม่แตะโมเมนต์ ไม่แตะรูปต้นฉบับ**
- ท้ายหน้ามีแถบ **"รูปที่เอาออกจากเล่ม"** แตะเอากลับได้ทุกใบ
  ⚠⚠ **ห้ามเปลี่ยนเป็นลบจริงเด็ดขาด** — ลบแล้วเอาคืนไม่ได้คือของอันตรายที่สุดในแอปนี้
- **เปลี่ยนรูป** = เพิ่มใบใหม่ + เอาใบเก่าออก + **ย้ายใบใหม่ไปนั่งตำแหน่งเดิม** (จด `pbShotPos` ไว้ก่อน gather ใหม่)
- `pbOrderOf`/`savePbOrder` เคยใช้ `ebTripId()` — แก้เป็น `pbTripId()` แล้ว ไม่งั้นลำดับที่จัดไว้หาไม่เจอ

⚠⚠ **เรื่องความละเอียด — อย่าลดพิกเซล** เจ้าของบอกว่า *"ไม่ต้องทำเผื่อการพิมพ์"* แต่ก็บอกด้วยว่า
*"เปิดดูใน desktop / tablet / iPad อย่าให้แตก"* — สองอย่างนี้ไม่ได้ขัดกัน เพราะ

> **จำนวนพิกเซล = ตัวที่ทำให้ "ไม่แตก"** · **คุณภาพการบีบ = ตัวที่ทำให้ "ไฟล์ไม่ใหญ่"**

จอ iPad Pro 12.9" กว้าง 2732 พิกเซลจริง รูปเต็มหน้าจึงต้องการราว 2600 ขึ้นไป (บังเอิญพอๆ กับงานพิมพ์ A5 ที่ 300dpi = 2480)
**จึงคง 2600px ไว้ แล้วลดคุณภาพการบีบ 0.9 → 0.85 แทน** ไฟล์เล็กลงราว 25–30% ตาเปล่าบนจอแยกไม่ออก
ถ้าวันหลังมีใครจะ "ลดความละเอียดเพราะไม่ได้เอาไปพิมพ์" — **อ่านย่อหน้านี้ก่อน**

ทดสอบแล้ว (Node 22 ข้อ + ยิงจริงบนเว็บ 23 ก.ย. 69): เอาออก 128 → 127 การ์ดกับเล่มตรงกันทั้งคู่ ·
ใบที่เอาออกโผล่ในแถบท้ายหน้าถูกวัน · กดเอากลับได้ 128 เท่าเดิม · ไม่ทิ้งคีย์ขยะไว้

ทดสอบแล้ว (Node 13 ข้อ + ยิงจริงบนเว็บ 23 ก.ย. 69): การ์ด 126 → 127 · รูปใหม่เข้า "วันที่ 1" ถูกต้อง ·
ช่องในเล่มรวม 127 **เท่ากับตัวเลขบนการ์ดพอดี** · เพิ่มใบซ้ำไม่เพิ่มซ้ำ · ทริป/วันอื่นไม่ปนกัน · ลบข้อมูลทดสอบออกแล้ว

## ชั้นหนังสือ = เล่มจากทริปเท่านั้น (20 ก.ย. 2569)

เคยเอาเล่มที่ทำจากเรื่องเล่าขึ้นชั้นด้วย แล้วพบว่า **ซ้ำกับแท็บ "เรื่องเล่า" ที่อยู่ข้างกันในหน้าเดียวกัน** และท่วมจอ (71 การ์ด)
→ เล่มจากเรื่องเล่าเข้าถึงได้ที่หน้า **"จัดการบันทึกเรื่องเล่า" ของเรื่องนั้น** (ปุ่ม "รวมเป็นอีบุ๊ก · จัดรูป · ไฟล์ EPUB")
โค้ดเดิมอยู่ในประวัติ commit ถ้าจะเอากลับมาต้องแยกเป็นแถวของตัวเองหรือมีปุ่มพับเก็บ

## ตัวนำเข้า WordPress = ของเจ้าของคนเดียว

เจ้าของมีบล็อก nimjourney 15 ปี 400+ โพสต์ นำเข้ามาเป็นเรื่องเล่า ~68 เรื่อง
**ผู้ใช้คนอื่นไม่มีแบบนี้** — เจ้าของพูดเองว่า *"user คนอื่นจะไม่มีการดึงข้อมูลจาก wordpress แบบเราหรอก"*

- **อย่าลงแรงเพิ่มฝั่งนี้** โดยเฉพาะ **อย่าทำ Worker ดึงรูปจาก `files.wordpress.com`** — เคยออกแบบไว้แล้วตัดสินใจไม่ทำ เพราะแก้ปัญหาให้ผู้ใช้คนเดียว
- เล่ม 68 เล่มนั้นคือ**ของตั้งโชว์** ไม่ใช่ฟีเจอร์

## รูปจาก WordPress: แสดงได้ แต่ใส่ EPUB ไม่ได้ (ทดสอบแล้ว 20 ก.ย. 2569)

ตอนนำเข้า แอปอ้างรูปจาก `files.wordpress.com` ตรงๆ ไม่ได้ก็อปมาเก็บ (ตั้งใจ — ประหยัดที่)
ทดสอบจาก origin `onevela.net` กับรูปจริง:

| วิธีเรียก | ผล |
|---|---|
| `<img src>` | **โหลดได้** — นี่คือเหตุผลที่รูปขึ้นในแอปและในเล่ม A5 ปกติ |
| `fetch()` | ไม่ผ่าน — ไม่มี `Access-Control-Allow-Origin` |
| `fetch` no-cors | ได้ opaque อ่านเนื้อไฟล์ไม่ได้ |
| `<img crossOrigin>` | ถูกบล็อก — ทางอ้อมผ่าน canvas ก็ตัน |

→ **EPUB ต้องก๊อปเนื้อไฟล์เข้า zip จึงใส่รูป WordPress ไม่ได้** ทางออกคือ **แนบรูปของตัวเองเข้าตอน** ผ่านปุ่ม "＋ อัปรูปจากเครื่อง" ในหน้า "จัดรูปในเล่ม"
รูปที่อัปเก็บเป็น ref `idb:` ในเครื่อง → `resolvePhoto` คืน data url → ฝังลง EPUB ได้จริง

⚠ **ยังไม่ซิงค์ข้ามเครื่อง** — จัดรูปบนเครื่องไหน ต้องกดออก EPUB จากเครื่องนั้น

## สมมติฐานที่ยังไม่ได้พิสูจน์

- *"คนไทยไม่ชอบเขียน แต่อาจทำ photobook กัน"* — เจ้าของเชื่อแบบนี้ **แต่ยังไม่มีข้อมูลผู้ใช้จริงมายืนยัน** อย่าเอาไปใช้เป็นข้อเท็จจริง
- เจ้าของชอบงานเขียนแบบอ่าน ebook เอง แอปเลยออกมาหน้าตานี้ — เป็นเหตุผลที่ถูกต้องสำหรับคนทำคนเดียว (ความชอบคือสิ่งที่พาให้ทำต่อได้เป็นปี) แต่ให้รู้ตัวว่ามันคือความชอบ ไม่ใช่ข้อมูลตลาด

## ประเด็นค้างที่ยังไม่ตัดสินใจ

- **EPUB เป็นพรีเมียม เล่ม A5 ฟรี** → คนใช้ฟรีได้รูปแบบที่อ่านยาก คนจ่ายได้รูปแบบที่อ่านสบาย ตั้งใจหรือบังเอิญ? (ยกไว้คุยวันหลัง)
- **ลิงก์แชร์เด้งเข้าแอปทั้งก้อน** — `cloudflare/vela-share-worker.js` ทำแค่การ์ดพรีวิวให้ไลน์/เฟซบุ๊ก แล้ว `location.replace()` เข้าแอป 1.3 MB คนกดลิงก์มาอ่านตอนเดียวต้องโหลดทั้งแอปก่อน **ยังไม่ได้วัดว่าช้าแค่ไหน — ต้องวัด ไม่ใช่เดา**

---

## แผนผังไฟล์

| ที่อยู่ | คืออะไร |
|---|---|
| `app/` | **ตัวแอป** — `index.html`, `sw.js`, `wtn-backend.js`, `photo-pick.js`, `supabase-config.js`, ไอคอน/manifest |
| `index.html` (root) | หน้าเว็บขาย + `help-*.html`, `privacy.html`, `support.js` |
| `cloudflare/` | โค้ด Cloudflare Worker + wrangler config + เอกสาร |
| `supabase/` | SQL ที่ต้องรันใน Supabase (`share-chapter.sql`, `books.sql`) |
| `capacitor/` | แอป native — `www/` คือสำเนาของแอปสำหรับบิลด์ |
| `codemagic.yaml` | ตั้งค่า CI บิลด์ iOS/Android |
| `SPEC-moment-picks.md` | สเปคเรื่องทำเล่มจากโมเมนต์ที่คัดไว้ + จาก WordPress |
| `SPEC-reminders.md` | สเปคระบบเตือน (เฟส 1 เตือนประจำวัน · เฟส 2 ชวนเรียบเรียงหลังกลับ) |
| `firebase/`, `firebase-config.js` | ของเก่าก่อนย้ายมา Supabase — ไม่ได้ใช้แล้ว |

## หลังบ้าน
- **ข้อมูล**: Supabase (โปรเจกต์ onevela) — anon key เปิดเผยได้ ปลอดภัยเพราะมี RLS คุม
- **รูป**: Cloudflare R2 bucket `vela-media` เสิร์ฟผ่าน Worker ที่ `onevela.net/i/*` — key ใช้ path เดียวกับ Supabase Storage (`users/<uid>/img/<file>.jpg`) Worker ย้ายรูปเข้า R2 เองตอนถูกเรียกครั้งแรก
- **ลิงก์แชร์**: Worker ที่ `onevela.net/s/*` (ตอน) และ `/b/*` (เล่ม) — ต้องมี RPC `share_chapter` / `share_book` ใน Supabase
- ⚠ โค้ดเรียก RPC `share_book_plan` ด้วย แต่ใน `supabase/books.sql` มีแค่ `share_book` — **ไม่พัง** เพราะมีทางสำรองอ่านจากตาราง `books` ตรง แต่ทุกครั้งที่เปิดลิงก์เล่มจะยิงคำขอที่ล้มเหลวไปหนึ่งครั้งก่อน
- **PDF worker** `/pdf/*` ยังไม่ได้ทำ (`pdfBase` ว่าง) — ดู "ตัดโรงพิมพ์" ข้างบน
- สวิตช์เปิด/ปิด worker อยู่ที่ `app/supabase-config.js` (`mediaBase` / `shareBase` / `pdfBase`) — ค่าว่าง = fallback กลับทางเดิม ไม่พัง

## โครงข้อมูลของเล่ม (eb*)

- `ebStory()` หาเรื่องที่จะทำเล่ม · `ebTripId()` คืน **ค่าว่าง** เมื่อเรื่องไม่ผูกทริป (ห้ามหล่นไปใช้ `state.trip` — เคยเป็นบั๊กที่ทำให้เล่มลากวัน/ค่าใช้จ่ายของทริปอื่นเข้ามาเงียบๆ)
- `ebKey()` = กุญแจเก็บปก/รูปที่จัดวาง — ใช้ `'st' + storyId` เมื่อไม่มีทริป
- `_htmlParas()` แบ่งเนื้อเรื่องเป็นท่อน · `_htmlBlocks()` ทำเหมือนกันแต่จำชนิด (หัวข้อ/ยกข้อความ/ตัวหนา) ไว้ด้วย
  ⚠ **จำนวนท่อนของสองตัวนี้ต้องเท่ากันเสมอ** เพราะรูปที่ผู้ใช้จัดวางไว้อ้างด้วยลำดับย่อหน้า ถ้าไม่เท่าโค้ดจะถอยไปใช้ข้อความเปล่าเอง
- `_epubAssets()` นับรูปที่ดึงไม่ได้แล้วเตือน — **ห้ามกลับไปใช้ catch ว่าง** เคยทำให้ได้เล่มไม่มีรูปโดยไม่มีใครรู้

## ⚠⚠ หยุดทำ Android แล้ว — เหลือ iOS กับเว็บ (24 ก.ย. 2569)

**เจ้าของตัดสินใจเอง หลังยื่นขอเผยแพร่บน Google Play แล้วไม่ผ่าน**
Google ตอบกลับว่าต้อง *"ทำการทดสอบแบบปิดที่มีผู้ทดสอบอย่างน้อย 12 รายเป็นเวลาอีก 14 วัน"* ก่อนจึงจะสมัครได้
สำหรับคนทำคนเดียวที่ผู้ใช้ยังน้อย นั่นไม่ใช่งานเขียนโค้ด แต่เป็นงานหาคน 12 คนมาเปิดแอปทุกวันสองสัปดาห์ — **ไม่คุ้มเวลาตอนนี้**

**คนใช้ Android ไม่ได้ถูกทิ้ง** ใช้เว็บ (`onevela.net` → "เพิ่มลงหน้าจอโฮม") ได้ครบทุกอย่าง ออฟไลน์ก็ได้ (มี service worker)
**สิ่งเดียวที่หายไปคือการเตือนตามเวลา** เพราะเบราว์เซอร์ตั้งนาฬิกาปลุกล่วงหน้าไม่ได้ → **จากนี้ตัวเตือนมีเฉพาะ iOS**

⚠ **ห้ามเสนอให้กลับไปทำ Android เอง** จนกว่าเจ้าของจะพูดถึงก่อน
⚠ **ห้ามลบอะไรทิ้ง** — แอปใน Play Console ปล่อยค้างไว้ (ไม่มีค่าใช้จ่าย) · โค้ดและคอนฟิกบิลด์ Android ใน `capacitor/` + `codemagic.yaml` เก็บไว้เหมือนเดิม วันไหนอยากกลับมาทำต่อได้เลย
⚠ **ข้อมูลเรื่อง Samsung ปัดแอปทิ้งแล้วนาฬิกาปลุกหาย ยังคงไว้** เผื่อวันหลังกลับมาทำ — แต่ตอนนี้ไม่มีผลกับใครแล้ว

### Web Push — ทางเดียวที่จะทำให้เว็บเตือนได้ (ยังไม่ทำ)

เจ้าของถามว่าทำไมแอปอื่นที่เป็นเว็บเตือนได้ คำตอบคือคนละกลไก

| | นาฬิกาปลุกในเครื่อง (Vela ใช้อยู่) | Web Push |
|---|---|---|
| ใครสั่ง | ตัวเครื่อง ตั้งล่วงหน้า 14 วัน | **เซิร์ฟเวอร์ยิงมา**ตอนถึงเวลา |
| ออฟไลน์ | เด้งได้ | ต้องมีเน็ต |
| ใช้ได้ที่ไหน | **แอปจากสโตร์เท่านั้น** | เว็บได้ (iPhone ต้อง Add to Home Screen ก่อน) |

ถ้าจะทำต้องมีสามชิ้น: กุญแจ VAPID · ขั้นตอนขออนุญาตและสมัครรับในแอป · งานตั้งเวลาฝั่งเซิร์ฟเวอร์ที่ยิงตามเวลาท้องถิ่นของแต่ละคน (ทำบน Supabase Edge Function ได้)
ตอนนี้ใน `sw.js` **ไม่มีตัวรับ push เลยสักบรรทัด**

⚠ **เจ้าของบอกว่า "เรื่องนี้เราไม่ซีเรียสมาก" (23 ก.ย. 69) — อย่าเสนอใหม่จนกว่าจะถามเอง**

## ⚠⚠ แนบ PDF โปรแกรมทัวร์ — ปุ่มที่ "รับไฟล์" แต่เซิร์ฟเวอร์รับไม่ได้ (24 ก.ย. 2569)

เจ้าของแนบ PDF โปรแกรมทัวร์ 19 หน้าแล้วสร้างแผนไม่ได้ **ไม่ใช่ไฟล์เสีย — ฟีเจอร์ทำไม่เสร็จแล้วปล่อยปุ่มค้างไว้**

ช่องแนบไฟล์เปิดรับ `image/*,application/pdf` แนบแล้วขึ้นชิปสวยงามเหมือนใช้ได้ แต่ `askLLMParts()` เตะทิ้งทันทีที่กด
⚠ **และเตะทิ้งแม้ผู้ใช้จะวางข้อความมาด้วย** — ขอแค่มีไฟล์แนบหนึ่งไฟล์ ทั้งคำสั่งล้ม
**นี่คือบั๊กประเภทที่แพงที่สุด: UI สัญญาสิ่งที่หลังบ้านทำไม่ได้** — ผู้ใช้จะคิดว่าแอปพัง ไม่ใช่ว่าฟีเจอร์ยังไม่มี

### ⚠⚠ ตัว AI จริงอยู่ที่ Supabase ไม่ใช่ Firebase

| ไฟล์ | สถานะ |
|---|---|
| `functions/index.js` (Firebase) | **ตายแล้ว** อย่าไปแก้ · เพดานในนั้น 12,000 ตัวอักษร ไม่ใช่ของจริง |
| Supabase Edge Function `ai-complete` | **ตัวที่ทำงานจริง** |

⚠⚠ **ซอร์สของ `ai-complete` ไม่ได้อยู่ใน repo เลย** อยู่แต่ในแดชบอร์ด Supabase → Edge Functions → ai-complete → Code
ของจริงข้างใน: `parts: [{ text: prompt.slice(0, 30000) }]` · `maxOutputTokens: 2048` · 1 คำขอ = 1 เครดิต · `MONTHLY = { year: 100, free: 0 }`
**ไม่มีช่องใส่ `inline_data`** = ส่งรูป/ไฟล์ขึ้นไปไม่ได้เลย ต้นเหตุอยู่ตรงนี้
→ *งานค้าง: ควรก็อปซอร์สลง `supabase/functions/ai-complete/index.ts` ให้อ่านได้จาก repo*

### ทางที่เลือก: แกะข้อความในเครื่อง ไม่แตะเซิร์ฟเวอร์

เจ้าของเลือกเอง (24 ก.ย. 69): *"เอาแค่ตัวอักษรพอ เรื่องรูปไม่ต้องเอาเข้า"*

`pdfToText()` โหลด **pdf.js จาก jsdelivr แบบ lazy** (เฉพาะตอนแนบ PDF) แกะชั้นข้อความในเครื่อง แล้วส่งขึ้นไปเป็นข้อความธรรมดา

- **ถูกกว่า** ส่งไม่กี่หมื่นตัวอักษร แทนไฟล์หลายเมกะไบต์ · **แม่นกว่า** ได้ตัวอักษรจริง ไม่ใช่ OCR · **ไม่ต้องแก้ `ai-complete`**
- ⚠ **pdf.js ยึดบัฟเฟอร์ที่ส่งเข้าไป** (โยนข้ามไปเธรด worker) ตัวเดิมใช้ต่อไม่ได้ — ต้อง `new Uint8Array(buf)` ส่งสำเนาเสมอ
- ⚠ **ปักหมุดเวอร์ชันไว้ที่ `PDFJS_VER`** ห้ามใช้ latest
- ⚠ **ไม่ได้ใส่ `cdn.jsdelivr.net` ใน `CACHEABLE_HOSTS` ของ sw.js โดยตั้งใจ** — เส้นทางแคชของ sw มี timeout 8 วินาที เน็ตอ่อนๆ จะกลายเป็นโหลดโมดูลไม่สำเร็จ ปล่อยให้เบราว์เซอร์แคชเองดีกว่า
- ⚠ **ช่องรับไฟล์ล็อกไว้ที่ `application/pdf` เท่านั้น** ห้ามเปิดกลับไปเป็น `image/*` จนกว่า `ai-complete` จะรับไฟล์ได้จริง ไม่งั้นบั๊กเดิมกลับมา
- PDF ที่เป็น**ภาพสแกนล้วน**จะได้ค่าว่าง → เด้งข้อความบอกตรงๆ ว่าไม่มีชั้นข้อความ **ห้ามเงียบ**
- เพดาน 40 หน้า · ชิปบอกจำนวนตัวอักษรที่แกะได้ และบอกด้วยถ้าอ่านไม่ครบทุกหน้า

ทดสอบกับไฟล์จริง (ทัวร์ Namibia 19 หน้า): ได้ 24,539 ตัวอักษร ใน 370 ms · เที่ยวบิน/เวลา/ที่พัก/หัวข้อรายวันครบ ·
อ่านซ้ำได้ · PDF เปล่าคืนค่าว่างไม่พัง · รวม 52 ข้อ ผ่านหมด · ยืนยันว่า pdf.js โหลดจาก CDN แล้ว worker ทำงานจริงบนเบราว์เซอร์

**ค้างอยู่ (เจ้าของบอกว่ายังไม่ต้องทำ):** เพดาน 30,000 ตัวอักษรในเซิร์ฟเวอร์ยัง `slice()` เงียบๆ
โปรแกรม 19 หน้านี้ 24,539 ตัวอักษรยังรอด แต่โปรแกรมที่ยาวกว่านี้จะโดนตัดท้ายโดยไม่มีใครรู้ แล้วได้แผนขาดวัน
⚠ ถ้าวันหลังเจอ "นำเข้าแล้ววันไม่ครบ" ให้สงสัยตรงนี้ก่อน

## ⚠⚠⚠ ai-complete คืน "ความคิด" ของโมเดลแทนคำตอบ — กระทบ AI ทุกฟีเจอร์ (24 ก.ย. 2569)

**ยังไม่ได้แก้ — ต้องแก้ที่ Supabase Edge Function `ai-complete`**

อาการที่เจอ: แนบ PDF ทัวร์ 19 หน้า แกะข้อความได้ครบ 24,539 ตัวอักษร แต่กด "ให้ AI แยกเป็นรายวัน" แล้วขึ้น
*"AI ตอบกลับมาไม่ครบหรือไม่ใช่รูปแบบที่อ่านได้"* ทุกครั้ง

**ยิงทดสอบผ่าน `WTNBackend.aiComplete()` จริง (บัญชี admin ไม่กินเครดิต) ได้ผลนี้**

| ขอให้ทำ | ได้กลับมา |
|---|---|
| ถามสั้น "เมืองหลวงนามิเบีย" | 19 ตัวอักษร ถูกต้อง |
| บรรยายยาว 300 คำ | 1,686 ตัวอักษร จบประโยคสวย |
| พิมพ์เลข 1–200 | **ขาดที่ 151** (645 ตัวอักษร) |
| JSON ทริป 15 วัน | **171 ตัวอักษร** ขาดกลางวันแรก |
| JSON ทริป 3 วัน | **240 ตัวอักษร และไม่ใช่ JSON เลย** |

ตัวที่ฟ้องชัดที่สุดคืออันสุดท้าย — ข้อความที่ได้กลับมาคือ **กระบวนการคิดของโมเดล** ไม่ใช่คำตอบ:
> `` `city`, `summary`, `stops` … * Language: Thai? Yes. * Country: Namibia? Yes. * 3 days? Yes.``

**สาเหตุ: โมเดลรุ่นนี้คิดก่อนตอบ แล้วโค้ดเอา "ความคิด" มานับเป็นคำตอบ**

```js
const text = (g?.candidates?.[0]?.content?.parts || []).map((p: any) => p.text || "").join("").trim();
```

`parts` มีทั้งก้อนความคิด (มี `thought: true`) และคำตอบจริงปนกัน — โค้ดต่อทุกก้อนเข้าด้วยกัน
และ **`maxOutputTokens: 2048` นับรวมโทเคนที่ใช้คิดด้วย** งานที่ต้องคิดเยอะจึงหมดโควตาตั้งแต่ยังคิดไม่จบ = ไม่มีคำตอบออกมาเลย

⚠ **นี่ไม่ใช่บั๊กของฟีเจอร์นำเข้าโปรแกรม** งานอื่นที่ให้ AI เขียนยาว (เรียบเรียงเรื่องเล่า จัดวัน) โดนแบบเดียวกัน
แค่ไม่มีใครจับได้ เพราะงานพวกนั้นไม่ได้ต้องการ JSON ที่ต้องปิดวงเล็บครบ — ได้ข้อความขาดๆ ก็ยังดูเหมือนคำตอบ
⚠ **แปลว่าฟีเจอร์นำเข้าโปรแกรมไม่เคยใช้ได้กับทัวร์ยาวๆ เลยตั้งแต่แรก** ไม่ใช่ของที่เพิ่งพัง

**แก้แล้ว 24 ก.ย. 2569 — deploy ขึ้นของจริงเรียบร้อย** (เจ้าของสั่ง *"แก้เลย"*) แก้ 3 บรรทัด เพิ่ม 19 บรรทัด ไม่แตะส่วนอื่นเลย

1. **กรองก้อนความคิดออกก่อนต่อข้อความ** — `.filter((p: any) => !p.thought)`
2. **`maxOutputTokens` 2048 → 8192**
3. **ถ้าไม่มีคำตอบเหลือเลย ให้ตอบ error ตรงๆ** (`code: "empty-answer"` · แยกกรณี `MAX_TOKENS` ออกมาบอกว่าให้แบ่งโปรแกรมสั้นลง)
   ⚠ `return` ก่อนหักเครดิต — **เรียกไม่สำเร็จต้องไม่คิดเงิน**
4. เพิ่มฟิลด์ `truncated` ในคำตอบปกติ (ฝั่งแอปยังไม่ได้ใช้ แต่มีไว้ให้ดูได้)

**ผลทดสอบหลังแก้ (ยิงผ่านระบบจริง บัญชี admin)**

| สั่งให้ทำ | ก่อนแก้ | หลังแก้ |
|---|---|---|
| เลข 1–200 | ขาดที่ 151 | **ครบถึง 200** |
| JSON ทริป 3 วัน | ไม่ใช่ JSON เลย (ได้บทคิด) | **2,394 ตัวอักษร · parse ได้ · 3 วัน** |
| JSON ทริป 15 วัน | 171 ตัวอักษร ขาดกลางวันแรก | **16,113 ตัวอักษร · parse ได้ · 15 วัน · 75 กิจกรรม** |
| บรรยายไทย 200 คำ | ปกติ | ปกติ ไม่มีบทคิดปน |
| ถามสั้น | ปกติ | ปกติ |

ตรวจของที่ห้ามพังแล้วทุกข้อ — ด่านพรีเมียม (`premium-only`) · ฟรี = 0 เครดิต · `no-credits` · การหักเครดิต ·
admin ไม่จำกัด · `peek` · เพดานขาเข้า 30,000 ตัวอักษร **ยังอยู่ครบเหมือนเดิม**

⚠ **ทริป 15 วันใช้เวลาราว 48 วินาที** ผู้ใช้จะเห็น "กำลังแยกวัน…" นานพอควร ถ้าวันหลังเจอ timeout ให้ดูตรงนี้
⚠ **Gemini ล่มชั่วคราวได้** ("This model is currently experiencing high demand") — เจอจริงตอนทดสอบ 1 ครั้งจาก 5 · กดใหม่แล้วผ่าน
⚠ **ยังไม่ได้พิสูจน์เคส `finishReason: "MAX_TOKENS"` ของจริง** — บังคับให้เกิดไม่ได้ในการทดสอบ (โมเดลหยุดเองก่อน) โค้ดเส้นนั้นจึงยังไม่เคยวิ่ง
⚠ **ไม่ได้ใส่ `thinkingConfig`** ตั้งใจไม่ใส่ เพราะถ้ารุ่นนี้ไม่รับพารามิเตอร์ API จะตอบ 400 แล้ว AI พังทั้งระบบ — ถ้าจะลดค่าคิด ต้องทดสอบแยกก่อน

**ถ้าต้องย้อนกลับ** ซอร์สเดิม (4,238 ตัวอักษร · sha256 ขึ้นต้น `2fcd5e61eec7abb6`) เก็บไว้ที่
`localStorage['__velaAiCompleteBackup']` บนโดเมน supabase.com ในเบราว์เซอร์เครื่องเจ้าของ
⚠ Supabase **ย้อน deploy อัตโนมัติไม่ได้** ต้องวางซอร์สเดิมกลับแล้วกด Deploy ใหม่
**ซอร์สอยู่ใน repo แล้ว: `supabase/functions/ai-complete/index.ts`** (24 ก.ย. 2569)
เจ้าของกด **Download** จากแดชบอร์ดแล้วเอาไฟล์มาวางใน `C:\vela` · คัดลอกเข้า repo แบบไบต์ต่อไบต์
119 บรรทัด · 6,910 ไบต์ · sha256 `a7f2fc2fe95a3edb8ccc4d0d7957ee41af00f69bb4f1936a144b6cc1b6b5a7f4` (ตรงกับที่ deploy อยู่)

⚠ **ไฟล์นี้เป็นสำเนาไว้อ่านและกู้คืน ไม่ได้ deploy อัตโนมัติ** — แก้แล้วต้องไปวางในแดชบอร์ดแล้วกด Deploy เอง
   (หรือใช้ `supabase functions deploy ai-complete` ถ้าวันหลังตั้ง CLI ไว้)
⚠ **ถ้าแก้ในแดชบอร์ด อย่าลืมกด Download มาอัปเดตไฟล์นี้ด้วย** ไม่งั้นสำเนาจะเพี้ยนจากของจริงแบบเงียบๆ
⚠⚠ **ห้ามพิมพ์ซอร์สใหม่จากภาพหน้าจอเด็ดขาด** สำเนาที่ผิดนิดเดียวแล้วมีคนเอาไป deploy อันตรายกว่าไม่มีสำเนา
   — ต้องได้ไฟล์จริงมาเท่านั้น แล้วเทียบ sha256 ทุกครั้ง

## ป้าย "พรีเมียม" บนการ์ดนำเข้าโปรแกรม (24 ก.ย. 2569)

เจ้าของสั่งเอง หลังพบว่าการ์ดไม่ได้บอกเลยว่าเป็นฟีเจอร์พรีเมียม คนใช้ฟรีจะแนบไฟล์เสร็จแล้วค่อยโดนเด้ง

| ส่วนไหน | ใครใช้ได้ |
|---|---|
| แนบ PDF + แกะข้อความ | **ทุกคน** (ทำในเครื่อง ไม่แตะเซิร์ฟเวอร์) |
| กด "ให้ AI แยกเป็นรายวัน" | พรีเมียม (`aiGate()` → `isPremium`) + เซิร์ฟเวอร์กันซ้ำอีกชั้น |
| "สร้างทริปจากแผน Vela" | **ทุกคน** ไม่ใช้ AI |

ป้ายผูกกับ `impNeedsPremium: !this.isPremium`
⚠ **ห้ามใช้ `aiAvailable()`** เพราะต้องรอ backend โหลดเสร็จ ป้ายจะกะพริบตอนเปิดแอป

**ค้างอยู่:** ในแอป iOS คนใช้ฟรีกดแล้วตัน — `aiGate()` เช็ค `isNativeApp` แล้วห้ามพาไปหน้าขาย (กฎ App Store)
ได้แค่ข้อความเด้งว่าเป็นฟีเจอร์พรีเมียม ไม่ได้บอกว่าต้องไปทำอะไรต่อ · ป้ายช่วยให้รู้ตั้งแต่แรกแล้ว แต่ทางออกยังไม่มี

## SEO ของเว็บขาย (24 ก.ย. 2569)

**Google Search Console: ยืนยันความเป็นเจ้าของแล้ว** พร็อพเพอร์ตี้ `https://onevela.net/` แบบ "คำนำหน้า URL"
ยืนยันด้วยไฟล์ `google1e72dfb144daee47.html` ที่รากรีโป (เนื้อในบรรทัดเดียว `google-site-verification: <ชื่อไฟล์>`)
⚠ **ห้ามลบไฟล์นั้น** ถ้าหายเมื่อไหร่ Google จะถอนการยืนยัน แล้วต้องทำใหม่ทั้งชุด

**ไฟล์ที่เกี่ยวกับ SEO ทั้งหมด**

| ไฟล์ | ทำอะไร |
|---|---|
| `robots.txt` | อนุญาตให้คลานทุกหน้า · ชี้ที่อยู่ sitemap |
| `sitemap.xml` | 8 หน้า (หน้าแรก · ศูนย์ช่วยเหลือ 6 หน้า · ความเป็นส่วนตัว) |
| `index.html` | JSON-LD ชนิด SoftwareApplication |
| `help.html` | JSON-LD ชนิด FAQPage (คำถามจริง 9 ข้อ) |
| ทุกหน้า | `<html lang="th">` |
| `app/index.html` | `<meta name="robots" content="noindex,follow">` |

⚠ **ห้ามใส่ `Disallow: /app/` ใน robots.txt** — ตัวแอปเป็นเปลือกเปล่า 1.2 MB ที่เนื้อหาเกิดจาก JavaScript
ถ้าห้ามคลาน Google จะไม่เห็น `noindex` ข้างใน แล้วจะเก็บ URL เปล่าไว้ในดัชนีแทน ซึ่งแย่กว่า
⚠ หน้าแอปตั้งใจ **ไม่บั๊มเวอร์ชัน** ตอนเพิ่ม meta ตัวนี้ (ไม่ได้แตะโค้ด เลยไม่ต้องให้ทุกเครื่องโหลด 1.2 MB ใหม่)

### ⚠⚠ ภาษาเริ่มต้นของเว็บขาย = ไทยเสมอ (`web-i18n.js`)

เดิม `pick()` เดาจาก `navigator.language` → ใครที่ตั้งเบราว์เซอร์เป็นอังกฤษจะได้หน้าอังกฤษ
**ปัญหาคือ Googlebot ตั้งภาษาเป็น en-US** เลยเห็นหน้าเป็นอังกฤษ ทั้งที่เราประกาศ `<html lang="th">`
= ภาษาที่ประกาศไม่ตรงกับเนื้อหาที่บอตเห็น ซึ่งเป็นสัญญาณลบตรงๆ · และคนไทยที่ตั้งมือถือเป็นอังกฤษก็เจอหน้าขายเป็นอังกฤษทั้งที่ตลาดหลักคือไทย

ตอนนี้ `pick()` คืน `'th'` เสมอ · เป็นอังกฤษเฉพาะคนที่**กดปุ่ม EN เอง** (จำไว้ใน `localStorage['vela-web-lang']`) · ตอนสลับ `boot()` ตั้ง `document.documentElement.lang = 'en'` ให้อยู่แล้ว
⚠ **ห้ามกลับไปเดาจาก `navigator.language`** ถ้าวันหลังจะทำสองภาษาจริงจัง ต้องแยก URL (`/en/`) + `hreflang` ไม่ใช่สลับเนื้อหาใต้ URL เดียว

⚠ **Cloudflare แคช `.js` ไว้ 4 ชั่วโมง** (`max-age=14400`) — แก้ `web-i18n.js` แล้วของเก่าจะยังเสิร์ฟอยู่จนหมดอายุ
ถ้าอยากเห็นผลทันทีต้อง Purge Cache ใน Cloudflare เอง · ส่วน `index.html` เป็น `DYNAMIC` (ไม่แคช) จึงเห็นผลทันที

**ค้างอยู่:** sitemap เพิ่งส่ง สถานะยังเป็น "ดึงข้อมูลไม่ได้" ซึ่งปกติสำหรับพร็อพเพอร์ตี้ที่เพิ่งยืนยัน — ตัวไฟล์เปิดได้จริงและ XML ถูกต้อง รอ Google มาอ่านเอง (เป็นวัน) ค่อยกลับไปดูสถานะ

## แอป native (iOS / Android)
บิลด์ด้วย Codemagic ไม่ต้องมี Mac · Bundle ID `net.onevela.app`

1. วางไฟล์ deploy ชุดล่าสุดไว้ที่ `_dist`
2. `node capacitor/sync-web.mjs` — ก็อปเข้า `capacitor/www/` แล้วปรับให้เหมาะ native (เติม `native.js`, ปิด https-redirect เมื่อรันใน Capacitor, ไม่เอา `sw.js` เข้าไป)
3. ถ้าขึ้นเวอร์ชันการตลาดใหม่ แก้ `VELA_VERSION` ใน `codemagic.yaml` — build number นับต่อจาก TestFlight ให้เอง
4. push → เข้า Codemagic **กด Start new build เอง** (branch `main`, workflow `Vela iOS · TestFlight`)

⚠ ทั้งสอง workflow เช็ค `capacitor/www/index.html` ก่อนเริ่ม ลืมขั้น 2 = fail ทันที

⚠ **Codemagic ไม่ได้บิลด์อัตโนมัติตอน push** (ไม่ได้เปิด webhook) — 21 ก.ย. 69 พบว่าบิลด์ล่าสุดค้างอยู่ที่ 8 ก.ย.
ทั้งที่มี commit หลังจากนั้นหลายตัว **push แล้วต้องเข้าไปกดเองเสมอ**

⚠ **`VELA_VERSION` ต้องขยับทุกครั้งที่ส่งรอบใหม่หลังตัวเก่าถูกปล่อยไปแล้ว**
ถ้าใช้เลขเดิม Apple จะตอบกลับตอน publish ว่า *"Invalid Pre-Release Train. The train version 'x.y.z' is closed for new build submissions"* (iris-code 90186)
บิลด์ ipa สำเร็จแต่ publish ล้ม — เสียเวลาไปหนึ่งรอบเต็ม

**นโยบาย: ไม่ต้อง sync native ทุกครั้งที่แก้เว็บ** — ทำเฉพาะตอนที่การแก้นั้นมีผลกับแอป native จริงๆ หรือตอนจะส่งรอบใหม่ การที่ `capacitor/www/` ตามหลัง `app/` อยู่เป็นเรื่องปกติและตั้งใจ ไม่ใช่บั๊ก

### Android / Play Store — สถานะ 21 ก.ย. 2569
- closed test **ผ่านเกณฑ์ครบแล้ว** (12 เทสเตอร์ · 14 วัน) ปุ่ม "Apply for production" เปิดแล้ว **ยังไม่ได้กด**
- ⚠ `codemagic.yaml` มี workflow แค่ `android-test` ซึ่งจบที่ `./gradlew assembleDebug` → **APK แบบ debug และไม่ได้อัปขึ้น Play** (ส่งเมลอย่างเดียว)
- production ต้องใช้ AAB ที่เซ็นด้วย**คีย์เดิมเป๊ะ** ถ้าเซ็นด้วยคีย์ใหม่ Play จะปฏิเสธ

**AAB ไม่ได้มาจาก Codemagic** — บิลด์บนเครื่องเจ้าของ (Windows, ต้องมี Android SDK) คำสั่งเดียวจบ

```powershell
cd C:\vela\capacitor
node release-android.mjs --build --name=1.0.6
```

⚠ **ต้องใส่ `--name=` เสมอ** ไม่งั้นสคริปต์จะหยิบ `appVersion` มาใช้ แล้ว versionName บน Play จะกลายเป็น `2026.09.21f` ซึ่งอ่านไม่รู้เรื่อง
สคริปต์ +1 `versionCode` ให้เอง · รัน `cap sync android` + `gradlew bundleRelease` ต่อ · ได้ไฟล์ที่ `android/app/build/outputs/bundle/release/app-release.aab`

⚠ **shell ที่ Claude ใช้บนเครื่องเจ้าของเป็น Linux sandbox ที่เห็นแค่โฟลเดอร์ที่ต่อไว้** — มองไม่เห็น Android SDK ใน `C:\Users\...\AppData\Local\Android\Sdk` จึง**บิลด์ AAB แทนไม่ได้** ขั้นนี้ต้องให้เจ้าของรันเอง

**สิทธิ์ที่ปลั๊กอินเตือนขอ** (เช็คแล้ว 21 ก.ย. 69) — `POST_NOTIFICATIONS` · `RECEIVE_BOOT_COMPLETED` · `WAKE_LOCK`
**ไม่มี `SCHEDULE_EXACT_ALARM`** จึงไม่ต้องกรอกแบบฟอร์มชี้แจงกับ Google

### ความยาวข้อความ "มีอะไรใหม่"
| สโตร์ | จำกัด |
|---|---|
| Google Play | **500 ตัวอักษร** |
| App Store | 4,000 ตัวอักษร |

**อย่าโฆษณาฟีเจอร์ที่มีแต่เจ้าของได้ใช้** — เจ้าของตัดบรรทัด "ทำอีบุ๊กจากเรื่องเล่าไหนก็ได้ ไม่ต้องผูกกับแผนเดินทาง" ออกเอง
เหตุผล: *"เรื่องเล่าคนอื่นไม่มี"* คนใหม่ไม่เคยเจอข้อจำกัดนั้น อ่านแล้วงงเปล่าๆ (ดู "ตัวนำเข้า WordPress = ของเจ้าของคนเดียว")

### อัปไฟล์ขึ้น repo ผ่านเบราว์เซอร์ — กับดักสองชั้น (21 ก.ย. 69)
เจ้าของอัปผ่านหน้าเว็บ GitHub ไม่ได้ใช้ git ในเครื่อง ถ้า Claude ทำให้ **ห้ามใช้ตัวแก้ไขของ GitHub วางทับ**

| วิธี | เกิดอะไร |
|---|---|
| `execCommand('selectAll')` + วาง | ❌ ไม่ได้เลือกอะไรเลย → **ไฟล์ซ้ำสองรอบ** |
| คลิปบอร์ด Windows | ❌ แปลง `\n` เป็น `\r\n` → ไฟล์โตขึ้นเท่าจำนวนบรรทัด |
| ตัวแก้ไข GitHub แก้ CRLF คืน | ❌ ทำไม่ได้ CodeMirror normalize ตอนโหลด เลยมองว่าไม่มีอะไรเปลี่ยน |
| **หน้า Upload files + `input.files` จาก `DataTransfer`** | ✅ ได้ไบต์ตรงเป๊ะ |

**ทุกครั้งต้องตรวจ SHA-256 ของไฟล์บน repo เทียบกับต้นฉบับหลังคอมมิต** อ่านไฟล์จริงผ่าน
`api.github.com/repos/<owner>/<repo>/git/blobs/<sha>` + header `Accept: application/vnd.github.raw`
(อย่าเชื่อ `raw.githubusercontent.com` ทันทีหลังคอมมิต — CDN คืนของเก่าอยู่หลายนาที)

## ทิศทาง/แบรนด์
- ชื่อแอป: "where to next?" / ทริปหน้าไปไหน
- โทน: อบอุ่น เป็นกันเอง — ครีม + ดินเผา (#c05f39), ฟอนต์ Chonburi (หัวเรื่อง) + Anuphan (เนื้อหา)
- ภาษา: ไทยเป็นหลัก
- แกนแอป = วางแผนรายวัน + งบ → เที่ยว → บันทึกเป็นเรื่องเล่า/รีแคป

## รายได้
- แอฟฟิลิเอต (ลิงก์จองในหน้าเพิ่มกิจกรรม: ที่พัก/เดินทาง/กิน/ที่เที่ยว) + AI พรีเมียม + ขายอีบุ๊ก/ร้านครีเอเตอร์
- AI: ใช้ AI ของแอป (ฟรีจำกัด/พรีเมียม) หรือผู้ใช้เชื่อม API key ตัวเอง (BYOK)
- ⚠ ขายสิ่งพิมพ์ **ตัดออกแล้ว** ดู "ตัดโรงพิมพ์" ข้างบน

## การเตือนบนมือถือ: ปัดแอปทิ้ง = ยกเลิกนาฬิกาปลุกทั้งหมด (21 ก.ย. 2569)

**ถ้ามีคนบอกว่า "ตั้งเตือนแล้วไม่เด้ง" ให้ถามข้อนี้ก่อนเสมอ ก่อนจะไปแตะโค้ด**

บน Samsung การปัดแอปทิ้งจาก recent apps มีผลเหมือน force stop → Android ยกเลิกนาฬิกาปลุกที่แอปตั้งไว้หมด
ทดสอบแล้ว: กด **Home** เฉยๆ → เด้งตรงเวลา · **ปัดทิ้ง** → เงียบทุกครั้ง

ครั้งแรกที่เจอ เสียเวลาไล่แก้โค้ดสามรอบทั้งที่โค้ดถูกมาตั้งแต่ต้น เพราะนัดทดสอบสั้นๆ (15 วิ) เด้งได้
ทำให้ดูเหมือนเป็นปัญหาของโค้ด — จริงๆ แค่ยังไม่ทันได้ปัดแอปทิ้ง

- **ตัดสินใจยังไม่ทำข้อความบอกผู้ใช้ Samsung** (22 ก.ย. 69) — เจ้าของขอรอจนมีคนแจ้งเข้ามาก่อน เพราะคนใช้ยังน้อย **อย่าเสนอใหม่** จนกว่าจะมีคนแจ้ง
  (แต่อาการนี้เงียบ คนเจอมักไม่แจ้ง — "ไม่มีคนแจ้ง" ไม่เท่ากับ "ไม่มีคนเจอ")
- รายละเอียดเต็มอยู่ใน `SPEC-reminders.md`

## จอคอมบนเว็บ — เปลี่ยนแนวแล้ว (22 ก.ย. 2569)

เจ้าของเปิดเว็บบนคอมแล้วขัดใจ เพราะโค้ดบังคับ `100vw!important` ทุกกรณี การ์ดทริปเลยยืดเป็นแถบยาวบนจอ 2291px
และเจ้าของบอกเองว่า **"ตอนพิมพ์แก้ไข ทำงานเขียนเยอะๆ ทำให้คอมสะดวกกว่า"** — คอมไม่ใช่ของแถม แต่เป็นที่ที่งานเขียนเกิดขึ้นจริง

**ทำไปแล้ว — `2026.09.22a`** กรอบมือถือจัดกลางจอบนเบราว์เซอร์ (`@media (min-width:900px)` + `html:not(.is-native)`)
แก้อาการยืดผิดรูป แต่**ไม่ได้ช่วยเรื่องพื้นที่เขียน** และเจ้าของดูแล้วบอกว่า "ไม่ใช่แบบที่คิด"

### ⏸ ออกแบบเสร็จแล้ว แต่ **เลื่อนไว้ก่อน** (22 ก.ย. 2569)

เจ้าของดูแบบร่างครบทั้ง 4 หน้าแล้วบอกว่า *"ถูกใจจนอยากทำทีเดียว"* แต่เลือก **"ขอลองใช้งานของเดิมไปก่อน"**
เข้าหลักเดิมของเจ้าของ — รอหลักฐานว่าคิดถึงอะไรจริงๆ ก่อนลงแรง (แบบเดียวกับปุ่มดาวและข้อความ Samsung)

**อย่าเริ่มทำเองจนกว่าเจ้าของจะบอก** แต่ถ้าเจ้าของหยิบขึ้นมา ของพร้อมหมดแล้ว: แบบร่าง ขอบเขต ลำดับ และหลักการข้างล่าง

**หลักการที่ใช้ตัดสินว่าหน้าไหนควรมีบนคอม** — ดูว่าเป็นงานประเภทไหน ไม่ใช่ว่าหน้านั้นสำคัญแค่ไหน

| ช่วง | เครื่อง | หน้า |
|---|---|---|
| วางแผน (ก่อนไป) | คอม | พิมพ์แผนรายวัน ใส่งบ |
| เก็บ (ระหว่างทาง) | **มือถือ** | เช็กอิน โมเมนต์ รูป ปุ่มดาว การเตือน |
| เรียบเรียง (หลังกลับ) | คอม | เขียน จัดรูปในเล่ม ออก EPUB |
| อ่าน/แชร์ | ทั้งคู่ | คนรับลิงก์เปิดจากที่ไหนก็ได้ |

มือถือไม่ได้ถูกลดบทบาท — มันรับช่วงที่คอมทำแทนไม่ได้

**ชุดโหมดคอม 4 หน้าที่อนุมัติแล้ว**

| หน้า | ทำไมต้องมีบนจอใหญ่ |
|---|---|
| **แผนของฉัน** | ทางเข้า · การ์ด 3 คอลัมน์แทนการซ้อนยาว |
| **หน้าเขียนเรื่องเล่า** | ปัญหาจริงของเจ้าของ · สามคอลัมน์: รายชื่อตอน · พื้นที่เขียน 680px · แผงรูป |
| **จัดรูปในเล่ม** | ได้ประโยชน์จากจอกว้างมากที่สุด — ลากรูปวางระหว่างย่อหน้า เห็นเล่มกับคลังรูปพร้อมกัน และ**ต้องทำบนเครื่องที่เก็บรูป `idb:` ไว้อยู่แล้ว** |
| **หน้าอ่าน / ลิงก์แชร์** | คนกดลิงก์จากคอมคือคนที่ยังไม่รู้จักแอป — ตรงกับ "ลิงก์ = ช่องทางโต" |

**หน้าที่เหลือ (ตั้งค่า เช็กอิน งบ นำเข้าจากบล็อก ฯลฯ) อยู่ในกรอบกลางจอแบบเดิม** เข้าถึงได้ปกติ ไม่ต้องทำ desktop layout
โหมดคอม = ชุดที่ออกแบบไว้เท่านั้น **ไม่ต้องมีครบเท่ามือถือ**

⚠ **แผนรายวัน + งบ** ตามหลักการข้างบนควรอยู่ในชุดด้วย (เป็นงานพิมพ์) แต่จงใจยังไม่ใส่ — รอดูว่าใช้ 4 หน้าแล้วคิดถึงมันไหม

แบบร่างที่เจ้าของอนุมัติ: Artifact **"Vela บนจอคอม — แบบร่าง"** (Design canvas, 4 artboard 1440×900) — หาได้ที่ claude.ai/code/artifacts

⚠ **ข้อควรระวังตอนลงมือ** — แอปมี `style="` ฝังในแท็ก **2,224 จุด** และ `wtn-screen` **35 หน้า**
ความกว้าง/ระยะห่างถูกเขียนตายไว้ทีละที่ **ห้ามไล่แก้ inline style ทั้งไฟล์** ให้ทำเป็น**ชั้นเสริม** —
media query + คลาสครอบเฉพาะหน้าที่เลือก เพื่อให้เส้นทางมือถือไม่ถูกแตะเลยและถอยกลับได้ด้วยการลบชั้นนั้นทิ้ง

**ลำดับที่แนะนำ** เริ่มที่หน้าอ่าน/ลิงก์แชร์ก่อน — เป็นหน้าอ่านอย่างเดียว ความเสี่ยงต่ำสุด
และได้เรียนรู้ว่าชั้นเสริมควรมีรูปร่างยังไงก่อนไปแตะหน้าเขียนซึ่งซับซ้อนที่สุด

## สถานะปัจจุบัน (21 ก.ย. 2569)
- เว็บ · iOS · Android อยู่ที่ `2026.09.21f` = **เวอร์ชันการตลาด 1.0.6** ซิงค์ตรงกันทั้งหมด

| ปลายทาง | เลข | สถานะ (21 ก.ย. 69) |
|---|---|---|
| เว็บ `onevela.net/app` | `2026.09.21f` | ปล่อยแล้ว |
| iOS | 1.0.6 (38) | TestFlight แล้ว · **ส่ง App Store รอตรวจ** |
| Android | 1.0.6 (18) | **closed testing** · ยังไม่กด Apply for production |

- ระบบเตือนเฟส 1+2 **ทดสอบเด้งจริงบนเครื่องผ่านแล้ว** (Samsung Z Fold) — สิทธิ์ผ่าน · ตั้งปลุกตรงเวลาผ่าน · คิว 14 วันครบ
  **เงียบเมื่อบันทึกแล้ว — พิสูจน์แล้ว 22 ก.ย. 69** บันทึกเมื่อวาน แล้วไม่มีการเตือนเด้ง (ข้อนี้ทดสอบแทนไม่ได้ ต้องรอข้ามวันจริง)
  ยังไม่ได้ลอง: แตะแล้วเข้าหน้าถูก · ย้าย timezone · เฟส 2 (ต้องรอทริปจบจริง)
- ข้อมูลจริงของเจ้าของ: 10 ทริป · 435 เรื่องเล่า (ส่วนใหญ่นำเข้าจากบล็อก) · 199 โมเมนต์ · เคยไป 46 ประเทศ
- เล่มบนชั้นหนังสือ: คาซัคสถาน (อีบุ๊ก + โฟโต้บุ๊ก), คลาสสิคโปรตุเกส, Harbin
- **ยังไม่มีผู้ใช้อื่นนอกจากเจ้าของ** — ทุกข้อสรุปเรื่องพฤติกรรมผู้ใช้ยังเป็นการเดา

## งานที่รอดูผลก่อนทำต่อ
- **ปุ่มดาวคัดโมเมนต์** (`2026.09.17a`) — คำถามคือ *"กดจริงไหม"* ไม่ใช่ *"โค้ดทำงานไหม"* ใช้จริงสัก 2 สัปดาห์ก่อนทำเฟสต่อไป (ดู `SPEC-moment-picks.md`)
- **รูป `{{ }}` 404** — `<img src="{{ ... }}">` ในเทมเพลตดิบยิงคำขอตั้งแต่ตอนเบราว์เซอร์อ่าน HTML ก่อน JS แทนค่า ไม่พังแต่ยิงทิ้งทุกครั้งที่เปิดแอป
