# สเปค: ระบบเตือน (Notifications)

> เขียน 21 ก.ย. 2569 · เจ้าของอนุมัติแนวทางแล้ว ("ทำตามที่คุณเสนอ")
> **ขอบเขต: ทำแค่เฟส 1 และ 2 เท่านั้น** (เจ้าของตัดสินใจ 21 ก.ย. 2569)
> สถานะ: **เขียนโค้ดเฟส 1 + 2 แล้วใน `2026.09.21a`** · ทดสอบตรรกะผ่าน 7 กลุ่ม · **ยังไม่ได้ทดสอบเด้งจริงบนเครื่อง**

---

## หลักการที่ทุกอย่างต้องยึด

**ศัตรูของแอปบันทึกไม่ใช่ "คนลืม" แต่คือ "ความรู้สึกผิด"**
แอปที่ทวงซ้ำๆ จะถูกปิดการแจ้งเตือนภายในสัปดาห์แรก แล้วไม่มีวันกลับมาเปิด

จากนั้นแตกเป็นกฎ 3 ข้อ

1. **เตือนตอนที่ลงมือได้ง่าย** ไม่ใช่ตอนที่ "ควรจะทำ"
2. **พกงานที่ทำไว้ให้ครึ่งหนึ่งมาด้วย** — มือถือรู้อยู่แล้วว่าวันนี้ถ่ายรูปอะไร ที่ไหน กี่ใบ เอามาใช้
3. **เงียบเมื่อเขาทำไปแล้ว** — ทวงสิ่งที่ทำไปแล้วคือวิธีทำลายความเชื่อถือที่เร็วที่สุด

---

## เฟส 1 · เตือนบันทึกประจำวัน (ทำก่อน)

**เวลา** ผู้ใช้ตั้งเองได้ · ค่าเริ่มต้น 20:30

เหตุผล: ช่วงกลับถึงที่พัก อาบน้ำเสร็จ ยังไม่หลับ — หน้าต่างเดียวที่คนเที่ยวว่างจริง

⚠ **ต้องใช้เวลาท้องถิ่นของที่ที่อยู่ ไม่ใช่เวลาไทย**
ไปไอซ์แลนด์แล้วเตือนตามเวลาไทย = เด้งตอนบ่ายสองที่นั่น ใช้ไม่ได้เลย
เป็นกับดักที่แอปท่องเที่ยวพลาดกันบ่อย — ต้องตั้งใหม่ทุกครั้งที่ timezone ของเครื่องเปลี่ยน

**เงื่อนไขยิง**
- วันนี้ยังไม่มีโมเมนต์/เช็กอินเลย → ยิง
- วันนี้มีแล้วแม้แต่อันเดียว → **ไม่ยิง**
- หนึ่งวันยิงได้ครั้งเดียว ห้ามทวงซ้ำ

**เนื้อความ — นี่คือหัวใจ ไม่ใช่รายละเอียด**

❌ ห้ามเขียนแบบสั่งงาน
> อย่าลืมบันทึกโมเมนต์วันนี้

✅ เขียนแบบยื่นงานที่ทำไว้ครึ่งทาง
> **วันนี้ที่เซี่ยเหมิน ถ่ายไป 47 รูป**
> แตะเพื่อเลือกสัก 3 รูป แล้วเขียนสองบรรทัด

ต่างกันระหว่าง **10 วินาที กับ 5 นาที** — และนั่นคือเส้นแบ่งว่าคนจะทำหรือไม่ทำ

ถ้าอ่านจำนวนรูปไม่ได้ (ไม่ได้ให้สิทธิ์คลังรูป) ให้ถอยไปใช้ชื่อสถานที่จากเช็กอินล่าสุด หรือชื่อทริป

**แตะแล้วไปไหน** เปิดหน้าเขียนโมเมนต์ของวันนั้นเลย ไม่ใช่หน้าแรก

---

## เฟส 2 · เตือนหลังกลับจากทริป ⭐

**อันนี้คุ้มที่สุด และแอปส่วนใหญ่ไม่มี**

**เวลา** 2–4 วันหลังวันสุดท้ายของทริป
(เร็วกว่านั้นยังเหนื่อยอยู่ · ช้ากว่านั้นความทรงจำจางแล้ว)

**เนื้อความ**
> **ทริปเซี่ยเหมินจบแล้ว — 12 โมเมนต์ · 340 รูป**
> เรียบเรียงเป็นเรื่องเล่าไหม

**ทำไมสำคัญ** นี่คือคำตอบของคำถาม *"คนใหม่จะไปถึงเล่มแรกได้ยังไง"*
ไม่มีใครนึกเองได้ว่าโมเมนต์ที่จดไว้กลายเป็นเล่มได้ — ต้องมีคนบอก
และนี่คือจังหวะเดียวที่บอกแล้วได้ผล

**เงื่อนไข** ทริปนั้นต้องมีโมเมนต์หรือเช็กอินอย่างน้อย 1 อัน ไม่งั้นไม่ต้องยิง

---

## ไม่ทำ — ตัดออกแล้ว (21 ก.ย. 2569)

_เก็บไว้เป็นบันทึกว่าเคยพิจารณาแล้วและตัดสินใจไม่ทำ · **อย่าเสนอใหม่** เว้นแต่เจ้าของเปลี่ยนใจเอง_

**ก่อนออกเดินทาง** — เตรียมกระเป๋า · พาสปอร์ตเหลือเกิน 6 เดือนไหม (คนลืมจริงและเสียหายหนัก) · วีซ่า · ซิม/ประกัน · เช็กอินออนไลน์ 24 ชม.ก่อนบิน

**ตามแผนระหว่างทริป** — มีแผนรายวันพร้อมเวลาอยู่แล้ว เตือนได้: ออกจากที่พักไปสนามบิน (เวลาบิน − เวลาเดินทาง − เผื่อ) · เช็กเอาท์ · ทัวร์ที่จองไว้

**เหตุผลที่ตัด** ข้อนี้เปลี่ยน Vela เป็น **"ผู้ช่วยระหว่างทาง"** ซึ่งเป็นคำสัญญาที่ใหญ่ขึ้นมาก —
เตือนพลาดแล้วเสียหายจริง (ตกเครื่องบิน เช็กเอาท์ไม่ทัน) คนจะโทษแอป
ไม่คุ้มจะรับความเสี่ยงนี้ตอนที่ยังไม่มีผู้ใช้

**งบ** — ถ้าวันหลังทำ ให้รวมไปกับการเตือนตอนเย็น **อย่าแยกเป็นอีกครั้ง**

---

## ห้ามทำ

- ❌ เตือนซ้ำในวันเดียวกัน
- ❌ เตือนกลางวันตอนกำลังเที่ยว — ขัดจังหวะสิ่งที่เขาอุตส่าห์เดินทางมาหา
- ❌ ภาษาตำหนิ *"คุณไม่ได้บันทึกมา 3 วันแล้ว"*
- ❌ **streak / นับวันติดต่อกัน** — ดูน่าสนุกแต่ทริป 7 วันเปราะมาก ขาดวันเดียวคนทิ้งทั้งทริป

---

## ข้อจำกัดทางเทคนิค

**สถานะ 21 ก.ย. 2569: เขียนแล้วใน `app/index.html`** — `notiCfg` / `notiLoggedToday()` / `notiDailyBody()` / `notiTripsDue()` / `notiSync()` / `notiBindTap()` + การ์ดตั้งค่าในแท็บ "ฉัน"

**ทำใน native ก่อน ไม่ใช่เว็บ**

| | native (Capacitor) | เว็บ |
|---|---|---|
| ตั้งเวลาในเครื่อง | `@capacitor/local-notifications` ทำได้เลย | ต้องมี push server |
| ค่าใช้จ่าย | ไม่มี | มี |
| iOS | ใช้ได้ | ต้องติดตั้งเป็น PWA ลงโฮมสกรีนก่อน ซึ่งคนส่วนใหญ่ไม่ทำ |

→ **เพิ่ม `@capacitor/local-notifications` ใน `capacitor/package.json` แล้ว** (^6.1.0) · ต้องรัน `npx cap sync` ก่อนบิลด์

**แบ่งงานได้เป็นสองชั้น** — ชั้นล่างทำในเว็บได้เลยไม่ต้องรอ native

1. **ชั้นตรรกะ** (เว็บทำได้) — ✅ ทำแล้ว หน้าตั้งค่าใช้ได้ทุกที่ บนเว็บจะเก็บค่าไว้เฉยๆ แล้วบอกตรงๆ ว่าจะเด้งเมื่อเปิดในแอป
2. **ชั้นส่ง** (ต้อง native) — ✅ ทำแล้ว ตั้งล่วงหน้า 14 วันทีละวัน (ไม่ใช้ repeat เพราะต้องข้ามวันที่บันทึกแล้ว) · ตั้งใหม่ทุกครั้งที่กลับมาหน้าจอ ทำให้ย้าย timezone แล้วเวลาตามไปเอง

**ข้อจำกัดที่ยังเหลือ** — ข้อความยังใช้ชื่อเมือง/ทริปแทนจำนวนรูป เพราะยังไม่ได้อ่านคลังรูปของเครื่อง (ต้องมีปลั๊กอินเพิ่ม) ถือเป็นทางถอยที่สเปคระบุไว้แล้ว ถ้าอยากได้ "ถ่ายไป 47 รูป" ต้องทำเพิ่ม

---

## ⚠ กับดักที่เสียเวลาไปครึ่งวัน — ปัดแอปทิ้งจาก recent apps

**ทดสอบบน Samsung Galaxy Z Fold (SM-F956B) 21 ก.ย. 2569**

อาการ: นัดทดสอบ 15 วิ / 40 วิ เด้งตรงเวลาทุกครั้ง แต่การเตือนจริงที่ตั้งไว้อีก 2–5 นาที **ไม่เด้งเลยสักครั้ง** (ลอง 11:43 · 11:55 · 12:00)

สาเหตุ: **การปัดแอปทิ้งจาก recent apps บน Samsung มีผลเหมือน force stop → Android ยกเลิกนาฬิกาปลุกของแอปทั้งหมดทันที**
นัดสั้นรอดเพราะยังไม่ทันได้ปัดทิ้ง

พิสูจน์: ตั้งเวลาอีก 3 นาที → **กด Home เฉยๆ** ไม่ปัดทิ้ง → เด้งตรงเวลา ✅

**บทเรียนสำหรับการทดสอบครั้งหน้า** เวลาทดสอบการเตือน ให้กด Home อย่าปัดแอปทิ้ง
และ**อย่าด่วนสรุปว่าโค้ดพัง** — ที่นี่เสียเวลาไล่แก้โค้ดสามรอบทั้งที่โค้ดถูกมาตั้งแต่ต้น

**ที่ยังต้องทำ** ผู้ใช้ Samsung ทุกคนจะเจอเรื่องนี้ (และยี่ห้อจีนหลายเจ้าดุกว่าอีก) —
ต้องมีข้อความในแอปบอกวิธีปลดล็อก: ตั้งค่า → แบตเตอรี่ → เอา Vela ออกจาก "แอปที่หลับ"/"แอปที่หลับลึก" · ตั้งค่า → แอป → Vela → แบตเตอรี่ → "ไม่จำกัด"

---

## เช็คลิสต์ทดสอบ

- [x] ตั้งเวลา → เด้งจริงตามเวลา **เวลาท้องถิ่น** ← ผ่าน 21 ก.ย. 2569 (ต้องกด Home ไม่ปัดทิ้ง)
- [x] สิทธิ์แจ้งเตือน Android 13+ → ขอแล้วผ่าน
- [x] ตั้งปลุกแบบตรงเวลา (`allowWhileIdle`) → ใช้ได้ ไม่ต้องขอสิทธิ์เพิ่ม
- [x] ตั้งคิวล่วงหน้า 14 วัน → `getPending()` คืน 14 รายการครบ
- [ ] เปลี่ยน timezone ของเครื่อง → การเตือนย้ายตาม ไม่ค้างที่เวลาเดิม
- [ ] บันทึกโมเมนต์ตอนบ่าย → ตอนเย็น **ต้องเงียบ** ← ข้อที่สำคัญที่สุดที่ยังไม่ได้ลอง
- [ ] ปิดสวิตช์ → ไม่เด้งเลย
- [ ] ไม่ได้ให้สิทธิ์คลังรูป → ข้อความยังใช้ได้ ไม่ใช่ข้อความว่าง
- [ ] แตะการเตือน → เข้าหน้าเขียนโมเมนต์ของวันนั้น ไม่ใช่หน้าแรก
- [ ] รีสตาร์ตเครื่อง → การเตือนยังอยู่
- [ ] เฟส 2: จบทริป → 2–4 วันถัดมาเด้งชวนเรียบเรียง · ทริปที่ไม่มีโมเมนต์ต้องไม่เด้ง

## บั๊กที่เจอระหว่างทาง (แก้แล้ว)

แม้ต้นเหตุจริงจะเป็นเรื่องเครื่อง แต่ระหว่างไล่หาเจอบั๊กจริงสองตัว **เก็บไว้ทั้งคู่**

| เวอร์ชัน | บั๊ก | ผลถ้าไม่แก้ |
|---|---|---|
| `21c` | `notiSync()` ที่ชนกันถูกทิ้งเฉยๆ (`if (this._notiBusy) return 'busy'`) | เปลี่ยนเวลาเตือนตอนแอปเพิ่งเปิด → ค่าใหม่ถูกบันทึกแต่ไม่เคยตั้งจริง เงียบโดยไม่มีสัญญาณ |
| `21d` | เปิดแอปทีไร ล้างคิวทิ้งแล้วตั้งใหม่ทุกครั้ง + ข้ามอันที่เหลือ < 30 วิ | เปิดแอปดูก่อนเวลาเตือนไม่ถึง 30 วิ → การเตือนของวันนั้นหายไป |

`21d` แก้ด้วยการจำลายเซ็นค่าตั้ง (`_notiSig`) — ถ้าไม่มีอะไรเปลี่ยนก็ไม่แตะคิวเลย และลดกันชนเหลือ 2 วิ
`21e` เพิ่ม `notiTestFire()` / `notiQueueText()` อ่านคิวจริงจากปลั๊กอิน · `21f` เอาปุ่มในแอปออกตามที่เจ้าของขอ
**แต่เมธอดยังอยู่ในไฟล์ ห้ามลบ** — ครั้งหน้าที่เงียบ ให้ผูกปุ่มชั่วคราวหรือเรียกจาก console จะได้ดูของจริงแทนการเดา

---

## ลำดับที่แนะนำ

1. ~~sync native ให้เท่าเว็บ~~ — ทำแล้ว `2026.09.21a`
2. ~~เพิ่มปลั๊กอิน + ขอสิทธิ์ + หน้าตั้งค่า~~ — ทำแล้ว (`@capacitor/local-notifications` ใน `capacitor/package.json`)
3. ~~บิลด์ native แล้วลองเด้งจริง~~ — **ผ่านแล้ว 21 ก.ย. 2569** บน Samsung Z Fold
4. **ใช้จริงสัก 1 ทริป** ← อยู่ตรงนี้ — ดูว่ากดจริงไหม ก่อนทำเฟส 2
5. เฟส 2 (เตือนหลังกลับ) แล้วจบ — ไม่มีเฟส 3
